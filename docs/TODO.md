# Last Stardust — 4번: 종합 TODO (우선순위 실행 목록)

> `docs/archive/REVIEW.md`(3번 갭 분석)의 후보들을 **실행 순서로 재배열**하고, 코드를 훑으며 내가 따로 발견한
> 이슈·결정 대기 항목을 붙였다. 우선순위는 **ROI(효과÷비용)** 기준 — "이미 도는 시스템에 훅 하나로 붙는 것"을 위로,
> "새 시스템/건축/모드 개조"를 아래로 놓았다.
>
> 표기: 난이도 🟢쉬움(KubeJS 훅 하나) · 🟡중간(새 스크립트/상태) · 🔴어려움(모드 개조·건축·리소스팩)
> 각 항목 = **[무엇] · 왜 · 어디(파일) · 난이도 · 의존**. 체크박스는 착수용.
>
> **⚠️ 실전 미검증** 표시가 붙은 항목이 많다. 인게임에서 확인할 순서와 명령은
> **`docs/TEST-PLAN.md`** 에 정리돼 있다 — 여기서 「구현했다」와 「도는 걸 봤다」는 다르다.
> 결정이 필요한 것은 `docs/DECISIONS.md`(열린 것 셋).

---

## A. 지금 바로 (P0 — 값싸고 효과 큼, 기존 시스템에 훅 하나)

> 진행 상태 (2026-07-24 유저 결정 반영): A1 제외 · A2/A3/A4 구현 완료 · A5/A6 보류.

- [~] ~~**밤 사냥 → 다음날 위협 -1**~~ · **제외** (유저 결정 — 굳이 불필요)
- [x] **부활 규칙(5초 무적 + 별빛 쇠약 60초, 최대 체력 -20% / 주는 피해 -25%)** · "읽을 수 없는 죽음" 완화 · **구현: `ls_revive.js` + `ReviveRules.java`**
  - 2026-07-30 수정: 쇠약의 공격력 감소가 `attack_damage` 모디파이어였는데 **그 속성을 보는 건 근접 평타뿐**이었다. 원거리 4종은 투사체 피해를 직접 계산하고, 8유물의 스킬은 전부 `LsDamage` 고정 수치라 **파티 화력의 대부분이 죽음의 대가에서 면제**되어 있었다. `LivingIncomingDamageEvent` 로 옮겨 구멍을 막았다. 무적도 공격 시 해제로.
  - 노브: `ReviveRules.GRACE_TICKS`/`FRAILTY_TICKS`/`FRAILTY_MULT` · 최대 체력은 여전히 `ls_revive.js`
- [x] **각성 충격파 1회** · 각성 순간 주변 몹 넉백 파동(피해 0)+폭발 파티클+사운드 · **구현: `ls_ascend.js` `asAscend`**
- [x] **성좌식 중계 문법(각성)** · `[ 꺼진 별 하나가 OO ]` 개인 표시 + 단계별 동사 승급(바라봅니다→주목합니다→빛나려 합니다), 기존 "각성했다!" 공지는 전체 유지 · **구현: `ls_ascend.js`** · ※ 구출/첫킬(`ls_rescue`/`ls_title`) 톤 통일은 미적용(원하면 추가)
- [x] 관문 클리어 "별이 돌아왔다" 연출 (A5) · **완료** — `ls_voice.js` 가 `rf_progress` 상승을 감지해 `ls_rift` 의 "봉인 해방" 타이틀 100틱 뒤에 두 번째 박자로 얹는다 (`ls_rift.js` 미수정)
- [x] 코드 이슈 점검(D절) (A6) · **완료** — `ls_siege.js finishSiege()` 의 `const accrued` 재할당 제거(`dawnReward` 분리) + 채팅/로그가 정규화 전 값을 출력하던 표시 버그 동시 수정. 전 스크립트 `const` 재할당 전수 조사 결과 나머지 3건은 오탐(다른 스코프의 `let`)

- [x] **마을 관리 화면 (`/town` UI화)** · 키바인드 **M** → 성역 재건 창. 공동 금고·위협도·성벽 내구도 게이지·4트랙 진행/비용·기여도 순위·성벽 보수 버튼.
      신규: `TownOpenPayload`·`TownDataPayload`·`TownActionPayload`·`TownState`·`TownScreen`·`TownScreenOpener`·`TownKeybind` · `/townui <json>` · `ls_town.js` 의 `twStateJson()`
      **데이터는 KubeJS가 소유** — 모드는 JSON을 받아 그리기만 한다. `TRACKS` 수치·문구 수정은 `/reload` 로 끝(모드 재빌드 불필요). 화면 배치를 바꿀 때만 재빌드.
      판정(비용·자원·상한)은 전부 KubeJS에 남겨 두 곳에서 갈리지 않게 했고, 버튼은 `town contribute/upgrade`·`wall repair` 명령으로 넘어간다.
      `/town list` = 채팅 대비책(모드 없음·콘솔용)
- [x] **인게임 아이콘 SMP 일괄 교체** · 9개 스크립트 39곳. 마크 기본 폰트(Unifont)는 BMP까지만 덮어서 🛡🔮🏘🧱🎲 등이 전부 □로 보이고 있었다 → ▦✧⌂▨⚀ 등 BMP 기호로 교체

- [x] **스킬 조작을 마우스 → 전용 키로 이전** · 좌클릭=평타 / **우클릭=비움**(솔라리스 스코프만 예외) / **R**=기본(1성) / **⇧⇧**=이동(2성) / **C**=추가(3성) / **X**=궁극(4성).
      해결한 충돌 3건: ①웅크림+좌클릭이 궁극이라 절벽에서 웅크린 채 때리면 60초 궁극기가 헛발 ②웅크리고 상자를 열면 웅크림이 블록 상호작용을 막아 상자 대신 스킬이 발동 ③우클릭이 비면서 도끼 껍질 벗기기·블록 설치가 정상 복구
      신규 `RelicKeybinds`(R/C/X, 재지정 가능) · `RelicActions.basicSkill()/extraSkill()` · `RelicInputPayload.BASIC/EXTRA` · 유물 8종의 `use()` 제거(솔라리스 제외)
      **솔라리스 R = 수동 재장전** — 6발 탄창이 빌 때까지 기다리면 반드시 교전 중 1.5초를 무방비로 서는데, 그 타이밍을 직접 고를 수 있게 됨(가득 찼으면 헛 재장전 차단)
      `CooldownDisplay` 에 키 라벨 추가 — `R 파동 3.4s · ⇧⇧ 돌진 준비 · C 반격 준비 · X 맹세 41s`
      ※ 게볼그 투창은 1초 차징이 사라지고 즉발이 됨(키는 홀드 상태가 서버로 안 넘어감). 되살리려면 누름/뗌 두 패킷 필요
  - [x] **이동기도 `V` 로 이전** · 더블쉬프트 감지 로직 제거 → `RelicInputHandler` 는 좌클릭 평타만 남았다. 웅크림 관련 오발동 3건 전부 해소

- [x] **소생(파나케이아 궁극) 실사용 불가 수정** · 원인은 대상 탐색이 `isDeadOrDying()` 뿐이라 **사망 화면에 그대로 떠 있는 사람만** 되살릴 수 있었던 것.
      실제로는 죽자마자 '리스폰'을 눌러버려서 힐러가 X 를 누를 땐 이미 스폰 지점에 살아 있다 → 항상 "되살릴 아군이 없다".
      게다가 **사망 화면이 채팅·타이틀·액션바를 전부 가려서 "기다려라"고 알릴 방법조차 없다.**
      → 신규 `ReviveManager` 가 사망 위치·시각을 장부에 적고, **30초 안이면 이미 리스폰했어도 쓰러진 자리로 되돌린다.**
      곁들여 고친 것: ①대상이 없으면 90초 쿨을 돌려줌(`clearCooldown`) ②위치·체력 적용을 15틱 뒤로 미뤄 KubeJS 리스폰 훅에 덮이지 않게 ③되살아난 사람의 '별빛 쇠약'(ls_revive.js) 해제 — 안 그러면 소생이 그냥 순간이동이 된다

- [x] **마을 시스템 모드 이관 + 데이터 기반 구축** · `LSData`(SavedData) 뿌리 + `TownCatalog`/`TownData`/`TownService`/`TownMenu` + 허브·트랙 2단 화면.
      **A안(창고형)** — 슬롯에 올려둔 자원이 서버에 남는다(창 닫아도·서버 껐다 켜도). 여럿이 나눠 채우고, 잘못 넣으면 도로 꺼낸다.
      개수를 세는 코드가 사라져 **오늘 하루를 잡아먹은 버그가 구조적으로 발생 불가능**해졌다.
      `ls_town.js`·`ls_income.js` → `.disabled`. 구조·이관 순서는 `docs/ARCHITECTURE.md`
- [x] **금고 이음새 차단** · `LSKubeBridge` 가 전역 `LS` 바인딩 등록 → 아직 스크립트인 `siege`·`rift`·`beacon`·`bounty`·`rescue`·`stats` 가 **모드의 금고 하나**를 본다.
      안 했으면 공성 보상이 옛 키에 쌓여 마을 창에 영영 안 보였다. 이관 완료 후 이 다리는 제거
- [x] **이관 잔재 청소 (2026-07-30)** · 주석 훑다가 나온 것들
  - **명예 보드가 조용히 틀린 값을 내보내고 있었다.** `ls_stats.js stTopContrib` 가 `town_c_<name>` 을 읽는데, 그 키를 쓰던 `ls_town.js` 는 이관되며 `.disabled` — **쓰는 쪽만 사라지고 읽는 쪽이 남았다.** 전원 0점으로 읽혀 `bv=-1` 탓에 **CSV 첫 사람이 항상 1위**. 예외도 로그도 없어서 아무도 몰랐다.
    → `LSKubeBridge.Api.topContributorName/Points` 추가(장부는 `TownData` 하나뿐이니 읽기도 거기서). `ls_bounty.js` 의 `town_cnames` 쓰기도 제거 — 읽는 사람이 없어졌다.
    ※ **교훈: 이관은 "쓰는 쪽을 옮겼다"로 안 끝난다. 읽는 쪽을 같이 안 옮기면 조용히 0을 읽는다.** 남은 이관에서 매번 확인할 것.
  - **`TownState.java` 삭제(138줄)** · 참조 0. `TownScreen`·`/townui` 와 함께 쓰이던 «KubeJS가 JSON 보내면 모드가 그리기만» 구조인데, A안(창고형) `TownMenu`/`TownService`/`TownView` 로 대체되며 통째로 남겨져 있었다. 위 A절 마을 화면 항목의 「신규」 목록은 그 시절 기록이라 지금 코드와 다르다.
- [~] **이관 계속 (순서: 성역좌표 → 진행도 → 가호/유물/각성 → 공성 → 나머지, `ls_voice` 최후)** · `docs/ARCHITECTURE.md` 참고 · 🔴
  - [x] **1단계 성역 좌표** · [x] **2단계 관문 진행도** · [x] **3단계 가호·유물·각성 (2026-07-31)**
  - 3단계 = `data/HeroData.java` + 다리 11종 + `ls_fate`/`ls_relic`/`ls_ascend` 28곳 교체.
    **앞의 두 단계에 없던 것이 하나 있다: `ls_migrate.js`.** 성역·진행도는 OP 명령 한 줄로 다시 세울 수
    있어 옛 키를 버렸지만, 여기는 여덟 명의 성장 기록이라 버리면 «전원의 가호가 사라진» 것이 된다.
    실제로 옮길 것이 있었다 — 가호 1 · 각성 1. 안 만들었으면 조용히 지워졌다.
  - [x] **4단계 공성 (2026-07-31)** · `data/SiegeData.java` 키 26개 + 다리 51종 + **바깥에서 읽던 6곳**.
    그 여섯이 이 단계의 전부였다 — 안 옮겼으면 희망 게이지가 늘 최대 · 호데고스 영원히 침묵 ·
    성벽 붕괴 대사 없음 · 최종 보스를 잡아도 폐막식 없음 이 되고 **아무 오류도 안 났다.**
  - [x] **5단계 나머지 6종 (2026-08-06)** · `bossdiff`·`title`·`bounty`·`beacon`·`casino`·`rescue`.
    섹션 6개 + 다리 약 60종 + 옛 키 옮기기 6종. **크기가 아니라 「키를 문자열로 조립한다」가 이유**였다:
    `'bd_' + id + '_hp'`(보스 id 의 콜론이 NBT 키) · `titles_<이름>` CSV · `'hunt|좀비|25|60'`.
    4단계와 똑같이 **읽는 쪽 셋**(`pb_names`→공성·희망, `town_pop`→폐막식)이 진짜 일이었다.
    이관하며 드러난 실제 버그 하나: **인구를 두 번 셀 수 있었다** — `rs_done_<키>` 와 `town_pop` 이
    따로 살았고 올리는 곳이 둘인데 둘 다 표식을 안 봤다. 지금 인구는 **명부의 크기**다.
  - [x] **6단계 `ls_voice` + 하늘 (2026-08-06)** · `data/VoiceData.java` + `SiegeData` 에 두 값.
    이 파일은 값이 **밖으로 안 샜다** — 앞선 다섯 단계와 다른 점이고, 옮긴 이유는 순수하게
    키 조립이다(`vc_once_<id>`·`vc_cd_<id>`·`vc_rot_<id>`·`vc_pend_<id>` — 대사 하나에 키 넷).
    같이 옮긴 `ls_time_locked`·`ls_nrate_pct` 가 **마지막 경계 넘김**이었다.
    → 그 결과 `ls_siege.js` 에 persistentData 접근이 하나도 안 남았다.

- [x] **이관 완료 (2026-08-06)** · 🎉 `LSData` 섹션 10개. **파일 경계를 넘는 persistentData 키 0개.**
  - **다리는 안 지운다.** 예전 `ARCHITECTURE.md` 에 「이관이 끝나면 `LSKubeBridge` 를 지운다」고
    적혀 있었는데 **그게 틀렸다** — 이관이 옮긴 건 «데이터의 주인»이고 판정·연출·명령은
    설계상 스크립트에 남긴 것이다. 그 스크립트가 장부를 읽으려면 다리가 필요하다.
    다리는 KubeJS 자체를 걷어낼 때 사라지고, 그건 계획에 없다.
  - [ ] **`ls_migrate.js` 삭제** (627줄) · 🟢 **이제 지울 수 있다 — 월드 리셋이 예정됐다(2026-08-06 확인).**
    이 파일이 하는 일은 **옛 persistentData 키를 모드 장부로 옮기는 것 하나뿐**이다.
    월드를 새로 파면 옮길 옛 키가 처음부터 없으므로 존재 이유가 사라진다.
    · 지우면 `/lsdata` 명령도 같이 사라진다 — **그건 아깝다.** 장부를 한 줄로 보는 유일한 창이라,
      지울 때 그 명령만 다른 파일(`ls_util.js` 쪽)로 옮기는 게 낫다.
    · 순서: **월드 리셋 → 한 판 돌려보고 → 그다음에 지운다.** 리셋 직전에 지우면
      「리셋이 잘못됐나, 이 파일을 지워서 그런가」를 못 가린다.

## B. 이번 반복 (P1 — 시그니처를 완성하는 중간 작업)

- [x] **호데고스 대사 테이블 + `ls_voice.js`** · **완료** — 3채널 함수(`vSys`/`vGuide`/`vWorld`/`vStar`/`vTitle`) 표준화 + 대사 10종.
      트리거: 전멸(5종 순환)·첫 공세 격퇴·관문 해방 T1~T4·위협도 밴드 2단·성벽 붕괴·밤 앰비언트·첫 접속·재접속 요약.
      **다른 스크립트 무수정** — `persistentData` 상태 전이를 1초마다 감시해 트리거를 만든다(파일을 지워도 게임은 그대로 돌아감).
      자발 발화는 토큰 버킷(4회, 3일마다 +1)으로 제한, 서사 분기점은 예산 무관. `/voice`·`/voice list`·`/voice test <id>`·`/voice mute`·`/voice budget`·`/voice reset`
  - [x] **성좌 중계 연결** · `ls_fate.js`(가호 선택 직후 1성) + `ls_ascend.js`(각성 시 해당 성급)에서 `vStar()` 호출. 둘 다 `try/catch` — `ls_voice.js` 가 없어도 가호 지급·각성은 완주한다. 각성의 성좌 문구는 개인 → **전체 중계**로 승격(STORY 부록A "전체 중계 — 성좌식")
  - [ ] **(남은 것)** 수기 12편, 오프닝 시퀀스(첫 합동 접속 "별이 꺼지는 밤")

- [x] **각성 단계별 스킬 재분배** · 1성 패시브+기본 / **2성 이동기** / 3성 추가 / **4성 궁극** / 5성 완성(새 스킬 없이 배율·체력 최대).
      `RelicSkills.ready()` 의 minStar 16곳 + `SAGE_STARS{1,3,5}→{1,3,4}` + 툴팁(`RelicEventHandlers`) + `ASCEND` 테이블·각성 안내문(`ls_ascend.js`).
      부수 정리: 유물 8종 헤더 주석이 더블쉬프트 슬롯 도입 전 내용이라 **스킬 이름 자체가 실제 코드와 달랐다**(예: 게볼그 "웅크림+우클릭 = 질풍 돌진" → 실제는 꿰뚫기) — 전부 코드에 맞춤
- [x] **공성 예고 하루 단계화 (2026-07-31)** · **구현: `ls_siege.js` `sdTick`** · ⚠️ 실전 미검증
  - 세 박자: 아침 액션바(붉은 하늘) → 저녁 `DREAD_DUSK`(11500틱, 천둥·하늘 어둠) → 밤 `DREAD_HORN`(12600틱, 뿔피리).
  - **긴장은 습격이 아니라 예고에서 온다.** 밤이 되어서야 알면 준비할 시간이 없고, 준비할 시간이 없으면
    공성은 «대응»이 아니라 «당함»이 된다. 하루를 통째로 물들이는 쪽이 습격 자체를 세게 하는 것보다 싸다.
  - `/siege dread` 로 지금 단계 확인.
- [x] **봉화 점화 물결 연출 (2026-07-31)** · **구현: `ls_beacon.js` `pbIgniteWave`/`pbWaveRing`** · ⚠️ 실전 미검증
  - 8단 × 5틱 × 고리당 28점. 고리가 지나간 자리에서 껍질 몹이 죽는다 — **연출과 판정이 같은 고리다.**
    따로 돌면 "빛은 지나갔는데 몹이 남아 있는" 장면이 나오고, 그게 정확히 정화가 가짜로 보이는 순간이다.
  - `/purifier wave` 로 단독 시연.
- [x] **희망 게이지(위협도의 짝) (2026-07-31)** · **구현: `ls_hope.js`** · ⚠️ 실전 미검증
  - `희망 = 관문 45% + 봉화 25% + 마을 30% − 패배×8` (0~100). **저장하지 않고 매번 계산한다** —
    별도 장부를 두면 어긋나고, 어긋난 게이지는 없는 것만 못하다. 지금 이건 항상 실제를 비춘다.
  - 사이드바에 `§b희망` / `§5위협` 두 줄. 서명이 바뀔 때만 다시 그린다(매 틱 그리면 스코어보드가 깜빡인다).
  - 단계 효과: 25+ 성역 재생 I · 50+ 공성 보상 +20% · 75+ 재생 II + 35%.
  - 로드 순서 주의: KubeJS 는 알파벳 순이라 `ls_hope` < `ls_siege` — 그래서 `hoRewardMult` 가 쓰이기 전에 정의된다.
- [ ] **균열의 능동 반격(전진 캠프)** · 위협 12+ 시 성역 방향 미니 스포너 → "낮=정리, 밤=방어" 리듬 · `ls_siege.js onNewDay:500` + 노드 장부 · 🟡 · 노드 배치(리셋 후)
- [x] **공성 몹 풀을 관문 티어로 교체 (2026-07-30)** · **구현: `ls_siege.js`** · ⚠️ 실전 미검증
  - `threatTier()` 삭제 → `waveTier(prog, threat)`. **양(마릿수)=위협도 · 질(몹 종류)=관문 진행도.** 예전엔 함수 하나가 둘 다 정해서, 관문을 아무리 깨도 공성 몹은 그대로였고 반대로 하나도 안 깨도 방치만 하면 광전사가 나왔다 — 진행의 대가가 공성에 전혀 안 보였다.
  - **방치 보정**: 위협도 10+ 면 질 +1단계(상한 5). 관문을 안 깨면 영원히 좀비만 오는 «안전한 정체»가 되어서, 방치에도 대가는 남긴다.
  - 5단계는 새 몹을 안 더하고 **밀도만** 올린다(광전사 i%4·망령 i%3). 종류를 더 늘리면 화면에서 구분이 안 된다.
  - `buildWave` 가 `prog` 를 **인자로** 받는다 — `/siege status` 가 "1명이면 몇 마리"를 가정해서 부르므로 안에서 서버 상태를 읽으면 예측이 실제와 갈린다.
  - 같이 고친 것: 진화 예고 D-카운트다운이 위협도 기준이라 무의미해져 **방치 보정 임계(위협 10)** 기준으로 교체 · `/siege status` 에 「몹 단계 N/5 — 관문 n/4」 줄 추가(안 보이면 "관문 깼는데 뭐가 달라졌지"가 된다) · `TIER_NEWS` 에 5단계 추가
  - 표기 주의: 명령 리터럴은 `status` 다(`info` 아님). 문서·주석이 `/siege info` 로 적혀 있었던 것을 2026-07-30 정정.
- [ ] **주간 별빛 금고(Great Vault)** · 주간 3목표→주말 1선택 보상. 재클리어 이유 · `ls_bounty.js` 확장(주간 트랙) · 🟡 · 없음
- [ ] **관문 입장권 어픽스** · 열쇠 던전에 랜덤 수식어 2~3종, 자발적 위협 상향→보상↑ · `ls_keys.js` + gateway json · 🟡 · 없음
- [x] **복구 커맨드 감사 (2026-07-31)** · **구현: `ls_enrage.js` `/enrage clear`·`/enrage now <n>`**
  - **이 항목이 지목한 둘(`/relic revoke`·`/ascend set`)은 이미 있었다.** D절 3번이 낡은 기록이었다.
    명령을 전수로 뽑고 나서야 진짜 구멍이 보였다 — `/enrage` 는 **끄는 방법도, 8분을 기다리지 않고
    시험하는 방법도 없었다.** 「없다고 적힌 것」을 고치러 갔다가 「있다고 아무도 안 적은 것」을 찾은 셈이다.
  - `enClear()` 는 장부를 비우기 **전에** 모디파이어를 제거한다. 순서가 반대면 누구에게 걸었는지 잊은 채
    영구 버프가 남는다.
  - `/enrage now <n>` 은 `start` 를 되감는다 — 값만 밀어 넣으면 다음 틱이 되돌려놓는다.
    8분짜리 검증이 30초가 됐고, 그 덕에 **소프트 격노가 이번에 실제로 검증됐다**(이그니스 14.0 → 22.4 → 14.0).
  - 곁들여: 격노 대상 정리 루프가 O(n²) 였던 것을 `seen` 집합으로 바꿈.
- [x] **각성 질적 계단 (2026-08-11)** · **구현: `Passive2.java` + 매니저 12곳 + `ls_ascend.js`** · ⚠️ 실전 미검증
      **이 항목의 「4성」은 낡은 기록이었다.** 재분배 이전(1·3·5성 해금)을 전제로 쓰였는데,
      재분배가 **이미 2성·4성을 채우고 5성을 비웠다** — `ls_ascend.js` 머리 주석이
      「매 단계에 새 버튼이 생긴다 — 4성까지는」이라고 스스로 자백하고 있었다.
      그대로 4성에 넣으면 4성이 둘이 되고 5성은 그대로 빈다. **그래서 5성에 붙였다.**
  - **열둘 전부 딜이 아니다.** 5성은 이미 `피해 ×3.0` 으로 숫자를 다 준다 — 여기서 딜을
    또 올리면 08-11 에 맞춘 12종 목표선(90~114)이 통째로 깨지고 열두 판을 다시 재야 한다.
    단일 표적 지속 DPS 기여가 전부 0 이고, 그게 이 설계의 합격 조건이다.
  - 설계 중 **두 안을 되돌렸다** — 타이탄 거인 보너스 상향(`/dummy` 더미가 최대 체력
    100만이라 이미 걸려 있다)과 네메시스 기세 획득 2배(기세는 전부 일도양단 배수로 간다).
  - 전문·검증 절차는 `docs/CLASSES.md` 「5성 패시브 2단」.
  - 일곱이 「받는 피해」 축이라 **별의 축복 때 만든 `/dummy hit` 계측기가 그대로 쓰인다.**

- [ ] **별의 축복 (유물 랜덤 부가효과)** · 🟢 **구현 완료 · 인게임 검증 통과 (2026-08-08).**
      **남은 것은 `/dummy` 실측 재보정 하나뿐이다.**
      확정본 `docs/BLESSING.md` · 결정 근거 `docs/DECISIONS.md` 4절. **더 물어볼 것이 없다.**
      · **슬롯 4칸** — 2성 상의 · 3성 무기1 · 4성 하의 · 5성 무기2 (각성 단계마다 하나씩)
      · **무기 10종 = 유물 · 방어 8종 = 상의·하의** · 축 안에서만 중복 금지
        → C(10,2) × C(8,2) = **1,260 조합** · **희귀도 없음(균등 추첨)**
      · **저장은 플레이어**(성급과 같은 구조 — 아이템 태그는 표시용 사본).
        스틱스 쌍단검·유물 분실·갑옷 교체가 전부 공짜로 해결된다
      · **해금: 공방 Lv2 「대장간 지원」**(`forge` 플래그) · 성소 Lv4 는 제단 강화(`blessing`)
      · 전용 UI `/bless` — `TownMenu`/`TownTrackScreen` 골격 재사용, 새 텍스처 0개
      · 리롤 두 축: **종류 = 별의 파편 1/2/3** · **수치 = 별먼지 3/5/8** (성급별 고정, 반복해도 안 오름)
        종류를 바꾸면 수치도 같이 다시 굴린다(최댓값 유지 쇼핑 차단)
      · 수치 곡선 `u = random()^k`, k = 3성 2.5 / 4성 1.0 / 5성 0.4 — 초반 저확률·후반 고확률이
        여기서 나오고, 3성 최댓값 확률이 1/N² 이라 **노가다 브레이크를 겸한다**
      · 보류 3종(운석 파편·별의 도약·재림)은 Java 훅이 더 필요해서 나중에
      · 전부 Java (`ARCHITECTURE.md` 원칙 1). `RelicEventHandlers` 에 걸 훅이 **이미 12개 있다**.
        `BleedManager.apply()` 는 이미 있고 아무도 안 써서 **출혈은 거의 공짜다**
      · ⚠️ **퍼센트는 전부 책상 계산이다.** 구현 후 `/dummy` 로 재보정할 것 —
        특히 **연쇄·별빛 보호막**(조건부 진폭이 제일 크다)
      · ✅ **`BlessingCatalog.java` 완료 (2026-08-08)** — 18종 범위표 · `curve(star)` · 리롤 비용 ·
        슬롯 4칸 · 부수 상수 · 해금 플래그 + lang 42키(ko/en).
        **`ls_config.js` 가 아니다** — 서버 스크립트라 클라가 못 읽는데 스핀·툴팁이 이 표를 쓴다
        (`TownCatalog` 를 KubeJS 에서 옮겨온 것과 같은 이유). BLESSING.md 9·10절 정정됨
      · 🟡 · 남은 것: 저장(`LSData` 플레이어 장부) · 효과 18종 · `/bless` UI

- [x] **마을 트랙 자원 재조정** · ✅ **완료 (2026-08-08)**
      · `Level` 이 아이템 **1종**만 받던 걸 `List<Req>` 로 — 「돌 + 나무」 같은 복합 요구가 된다.
        태그도 받는다(`minecraft:logs` = 아무 나무나). 판정은 `Req.matches` 한 곳으로 모았다
      · 제출칸 **3 → 18**(2줄 × 9) — 최대 요구가 「석재 512 + 원목 256」 = 12스택이라 3칸으론 못 채운다
      · 수량 4~8배 ↑ — 이전 값은 4인 기준 몇 분이면 끝나서 **사실상 Ducat 만이 게이트**였다.
        목표는 「4인이 한 세션 = 한 레벨」, 전체 완주 약 10~12시간
      · 트랙별 색: 방벽=건축자재(양·제련) · 공방=금속(깊이) · 성소=희귀재료 · 구역=거래품
      · **조약돌은 양으로 게이트가 안 된다**(1인당 분당 수백 개) → 방벽 2부터 석재·심층암 벽돌
      · Ducat 은 안 건드렸다 — 공성이 4일에 한 번이라 이미 시간 게이트다. 두 게이트가 독립적으로 걸린다
      · 손댄 파일 9개: `TownCatalog` `TownData` `TownView` `TownService` `TownMenu` `TownGui`
        `TownTrackScreen` `TownHubScreen` `LSCommands` + lang 2개

## C. 나중에 / 리셋 후 (P2~P3 — 건축·모드 개조·운영 정책)

**콘텐츠 확장 (중간~어려움)**
- [x] **9·10번째 가호 「헤카테」·「하르모니아」 인게임 구현** · ✅ **코드 완료 (2026-08-09, `e7fa377`)**
      · 사양·갱신 내역 → **`docs/CLASSES.md 「헤카테·하르모니아」`**
  - 유물 2종 · 스킬 8개 · `CurseManager`/`HarmonyManager` · 각성 곡선 · 칭호 · lang · 디스코드 카드 갱신.
  - ⚠️ **인게임 검증 전혀 안 됨.** 공격력도 전부 가정치다 — 헤카테 `weapon(4.2,-2.4)` ·
    하르모니아 `(2.5,-2.2)`. `/dummy` 10종 재보정 전까지는 아무 수치도 믿지 말 것.
  - ⚠️ 모델 둘 다 **자리 맡기**다(같은 낫을 재채색). `models/item/*.json` + `textures/item/*.png`
    두 파일만 덮으면 교체된다 — 코드는 안 바뀐다. 출처·라이선스는 `docs/ASSETS.md`.
  - ⚠️ `CLASS-RESEARCH.md` §6 은 「9·10번째 추가 하지 말 것」이다. **유저가 알고 덮은 결정**이므로
    되돌리지 말 것. 다만 그 논거(인원 < 직업 수)는 여전히 유효한 위험이다.
  - 하르모니아가 들어갔으므로 `CLASS-RESEARCH.md` §6 의 「버퍼 분화 → 쿠 훌린 흡수」안은 **폐기**(중복).
  - 남은 것: `STORY.md` 5장이 아직 「8가호」 서술 · `art/Harmonia.png` 없음 ·
    `discord/클래스_스탯카드.txt` 보류(유저 지시).
- [ ] **제단 자연생성 (리셋 시)** · 🟡 · 가호별 유물 제단 9~10곳을 월드젠으로
  - **지금은 건물이 아니라 lodestone 한 칸**이다. `/relic altar <가호>` 로 좌표를 손으로 등록하고
    그 블록을 우클릭해 유물을 받는다(`ls_relic.js`). 즉 누군가 직접 짓고 등록해야 한다.
  - 방법: 데이터팩 구조물(`data/lsrelics/worldgen/structure` + `structure_set` + `template_pool` + `.nbt`).
    **위치 등록이 필요 없어진다** — `level.findNearestMapStructure(...)`(`/locate` 가 쓰는 그것)로
    물어보면 되므로 `/relic altar` 자체가 사라진다. 「원정 제단」(`ls_rift.js`)이 이미 좌표를 뿌리는
    구조라 그쪽과 결이 같다.
  - ⚠️ **왜 리셋 때인가** — 구조물은 **이미 생성된 청크엔 안 생긴다.** 지금 넣어도 성역 주변엔
    안 나오고 멀리 나가야 나온다. 리셋 때 넣어야 제값을 한다.
  - 건물은 하나만 만들어 **9~10번 재사용하고 가호 색 블록만 바꾸는 게** 낫다 — 따로 짓는 것보다
    훨씬 적은 일이고, 「같은 양식의 제단」이 세계관상 더 맞는다.
  - `.schem`(WorldEdit·PlanetMinecraft)은 그대로 못 쓴다. 붙여넣은 뒤 **구조물 블록으로 저장**해
    `.nbt` 로 뽑아야 한다.
- [ ] 밤 전용 보상 ①밤 채집물 ②밤 균열틈 미니던전 · `ls_keys`/월드젠 연동 · 🟡🔴
- [x] **봉화끼리 최소 거리 96m (2026-07-31)** · **구현: `ls_beacon.js` `PB_MIN_DIST`** · ⚠️ 실전 미검증
  - 원래 항목은 「노드 반경 확장 불가」였는데, **균열 노드에는 좌표가 없다**(`ls_nodes_names` 는 이름 CSV뿐).
    그래서 그대로는 못 만든다. 대신 같은 구멍의 진짜 크기를 재보니 이랬다 —
    봉화 2기당 위협 하한 −1 인데 **위치 조건이 하나도 없어서**, 성역 앞마당에 여덟 기를 세우면
    하한이 4 내려간다. 그건 영토 수복이 아니라 금고를 위협도로 바꾸는 환전이다.
  - 정화 반경(24)의 4배. 서로 안 닿을 만큼은 떨어지되 하루치 원정으로 닿는 거리 — 더 멀면
    「가능하지만 아무도 안 하는」 시스템이 된다.
  - 비용 검사보다 **먼저** 본다. 뒤에 두면 금고를 깎고 나서 거절한다.
  - 기존 봉화는 그대로 둔다(소급 철거 없음). 노드 좌표가 생기면 그때 노드 반경 규칙을 더한다.
- [ ] 균열 침식(오염 블록 주 단위 확산 ↔ 봉화 회복) · 스케줄러 `fill` · 🟡
- [ ] 광량-난이도-드롭 공식 · `EntityEvents.spawned` 광량 체크 · 🟡
- [ ] 돌발 이벤트 "유성 낙하" · 🟡
- [ ] 전초 봉화(리스폰 앵커) · `ls_beacon` 변종 · 🟡
- [x] **전멸 천장 「별의 유예」 (2026-07-31)** · **구현: `ls_rift.js`** · ⚠️ 실전 미검증
  - 같은 관문에서 3회 전멸하면 그 관문에 한해 **저항 I**. 깨면 장부·효과 둘 다 걷힌다.
  - 관문은 진행 게이트다. 막히면 각성·세계 등급·최종장이 전부 멈추고, 여덟 명이 시간을 맞추는
    서버에서 「이번 주도 못 깼다」가 세 번 겹치면 다음 주엔 네 명만 온다. **실력이 아니라 일정으로
    죽는 콘텐츠**가 되는 걸 막는 장치다.
  - **주는 피해가 아니라 받는 피해를 줄인다.** 보스 체력은 「4명·60초」에서 역산한 값이라
    화력을 올리면 그 계산이 통째로 무효가 되고 다음 측정에서 원인을 못 가린다. 저항은 싸움을
    길게 할 뿐 체력 목표를 건드리지 않는다.
  - 속성 모디파이어가 아니라 **바닐라 저항 효과**를 쓴다 — `attack_damage` 류는 근접 평타만 보고
    원거리 4종·8유물 스킬이 전부 빠져나간다(「별빛 쇠약」이 그렇게 반쪽으로 돌았다, A절 부활 규칙).
  - **한 계단에서 멈춘다.** 전멸마다 세지면 「일부러 지는 게 이득」이 된다. 천장은 바닥을 까는 것이지
    길을 만드는 게 아니다.
  - 전멸 감지는 `ls_voice.js` 것을 부르지 않고 따로 둔다 — 그쪽은 「다른 스크립트를 수정·의존하지
    않고 감시만 한다」는 설계라 역방향 호출이 그 성질을 깬다. 조건도 다르다(저쪽은 언제든, 여기는 교전 중만).
  - `/expedition pity` (조회) · `/expedition pity <n>` (설정, 개봉된 제단 필요) · `reset` 이 같이 비운다
- [ ] 5성 이후 유물 외형 진화(수평 성장) · `lsrelics` + 업적 카운터 · 🔴

**보스 레이어 (어려움 — 별도 스코프)**
- [x] **기믹 어휘 사전**(빨강=피해라 / 노랑=뭉쳐라 / 보라=흩어져라 / 초록=지금 쳐라 / 파랑=멈춰라, 예고 1.5초 고정) · **구현: `Telegraph.java`**
  - 다섯 색이 끝이다. 앞의 셋은 «어디에 설 것인가», 뒤의 둘은 «언제 때릴 것인가»를 말한다.
    초록·파랑은 대부분 **모드가 이미 가진 규칙을 보이게 만든 것**이다(강철거인의 2.9초 취약 창,
    이그니스의 반격 구간) — 없던 규칙을 더한 게 아니라 안 보이던 규칙에 색을 입혔다.
  - 테두리만 그린다(채우면 셰이더에서 지면과 뭉개지고 그 위의 몹이 안 보인다). 남은 시간이 짧아질수록 촘촘해져 초읽기가 눈에 보인다. 소리는 음높이로, 글자는 액션바로 — 파티클이 가려지는 상황(밤·셰이더·군중)에 대비해 3중.
  - 노브: `Telegraph.WARN_TICKS`(전 관문 공통 — 보스마다 다르게 두면 어휘가 아니라 개별 암기가 된다)
- [x] **T1 기믹 1종: 강철거인 「대지 가르기」** · **구현: `WroughtnautGimmick.java`**
  - 교전 8초 후부터 14초마다, **무작위 플레이어 발밑**에 반경 4.5 DANGER 장판 → 1.5초 뒤 피해 10 + 솟구침.
  - 보스 발밑이 아니라 플레이어 발밑인 이유: 강철거인은 근접 전용이라 원거리 유물 4종에게는 **가만히 서서 쏘면 이기는 과녁**이었다. 어휘를 파티 절반만 배우면 T3 조합에서 무너진다.
  - 자연 생성 개체에도 걸린다 — 기믹은 관문이 아니라 그 보스의 속성이다.
  - 시험: `/lsgimmick summon · now · test <danger|stack|spread> · clear`
- [x] **T2 기믹: 이그니스 「업화의 결계」(노랑)** · **구현: `IgnisGimmick.java`** · ⚠️ 실전 미검증
  - 교전 10초 후 16초마다, **파티 무게중심에서 6~9칸 떨어진 곳**에 반경 5 STACK 결계 → 1.5초 뒤 **원 밖**만 피해 12 + 화상 3초. 안쪽은 무피해.
  - 누군가의 발밑이 아닌 이유: 표적이 된 사람은 이미 안에 서 있어 아무것도 안 해도 되고, 1인 시험에서 기믹이 통째로 무효가 된다. 떨어진 곳에 띄워야 **전원이** 발을 뗀다.
  - 안쪽을 완전 무피해로 둔 건 빨강의 정확한 반대를 만들기 위해서다. 조금이라도 아프면 "노랑도 아프네"가 되어 어휘가 흐려진다.
  - 2페이즈(50%): 주기 11초 + 결계 연속 시전. 새 어휘 없이 같은 것을 더 빠르게만.
  - 시험: `/lsgimmick ignis <summon|now>`
- [x] **T3 기믹: 건틀렛 「분쇄 파문」(보라)** · **구현: `GauntletGimmick.java`** · ⚠️ 실전 미검증
  - 교전 9초 후 15초마다, **사람마다 반경 4 SPREAD 원 하나**. 1.5초 뒤 그 원 안에 둘 이상 남아 있으면 그 안 전원이 피해 7. 혼자면 무피해.
  - 판정이 "원 안이냐"가 아니라 "몇 명이 같이 있냐"인 이유: 원 안이면 피해로 만들면 그건 그냥 빨강이고, 어휘가 하나 줄어든다.
  - **1인 시험에서는 원만 뜨고 피해가 없다** — 버그가 아니라 정의상 그렇다. 확인하려면 2명이 붙어 서서 `now`.
  - 2페이즈(50%): 보라 + 보스 발밑 빨강(반경 6, 피해 9) 동시 = **어휘 조합의 첫 적용점**.
  - 시험: `/lsgimmick gauntlet <summon|now>`
- [x] **기믹 공통 뼈대** · **구현: `BossFightTracker.java`** — 개체 추적·교전 판정·유휴 되감기·페이즈 전환·시험 소환·전투 기록. 기믹은 「무엇이 일어나는가」만 쓴다.
  - **다섯 기믹 전부 이 하나를 쓴다 (2026-07-31 `WroughtnautGimmick` 이관 완료, 중복 약 150줄 제거).**
    미루던 걸 지금 옮긴 이유: 인게임 검증을 앞두고 **옛 코드를 검증하면 그 검증이 이관과 함께 버려진다.**
    그리고 아래 전투 기록계가 네 보스 모두에 필요한데, 안 옮기면 같은 루프를 한 벌 더 쓰게 된다.
  - 이관하며 뼈대에 더한 것 둘: `Fight.data`(기믹 전용 상태 — 강철거인의 취약 창 두 값이 공용 필드로
    올라가면 나머지 넷에게는 영영 안 쓰이는 칸이 된다) · `onTickAlways()`(교전 여부와 무관한 매 틱).
    **후자가 없으면 `/lsgimmick window` 가 죽는다** — 기존 `onTick` 은 교전 판정을 통과해야 오는데,
    표적 없는 시험 소환 개체가 정확히 그 경우다.
  - 함정 둘: (1) `Telegraph.outside()` 는 **월드 전체**를 훑는다 — 판정에 쓰면 성역에 있던 사람이 맞는다. 항상 교전 목록 기준. (2) `Telegraph` 의 resolve 안에서 다시 `Telegraph` 를 부르면 ACTIVE 순회 중 add 로 CME 가 나고, 그쪽 try/catch 에 먹혀 **조용히 안 터진다**. 연속 시전은 트래커 주기로 처리할 것.
- [x] **전투 기록계 (보스 처치 자동 보고)** · **구현: `BossFightTracker.report()`** · ⚠️ 실전 미검증
  - 보스가 죽으면 **걸린 시간 · 인원 · 실측 DPS · 그대로 붙여넣을 `/bossdiff abs` 한 줄**을 스스로 낸다.
    체력 목표가 «4명·60초»인데 그 60초를 여태 **사람이 초시계로** 재고, 나누고, 명령을 손으로 조립했다.
    세 단계 전부 틀릴 수 있고 더 흔한 사고는 «재는 걸 잊어서 보스를 한 번 더 잡는» 쪽이다.
  - 시간은 벽시계가 아니라 **교전으로 인정된 틱**이다. 죽고 돌아오는 시간까지 세면 체력만 부풀고
    그 시간이 통째로 «서 있는» 시간이 된다.
  - **운영 권한(2) 있는 사람에게만 보인다.** 일반 참가자에게 «권장 체력 4828»이 뜨면 보스전이 아니라
    스프레드시트가 된다. 콘솔 로그에는 항상 남는다.
  - 인원이 4명이 아니면 «이 값을 그대로 넣으면 어긋난다»를 같이 띄운다 — 조용히 틀린 값을 주는 것보다 낫다.
  - `/lsgimmick status` 의 교전 중 표시에도 「경과 N초」가 붙는다.
- [x] **`/bossdiff abs` — 절대 체력 모드** · **구현: `ls_bossdiff.js`** · %는 모드 원본 체력을 알아야 쓰는데 Mowzie's·Cataclysm 은 그 값이 코드 안이라 안 보인다. 튜닝의 입력은 «4명 1분»이고 거기서 나오는 건 절대값이다. 스폰 로그에 `base=` 도 항상 찍게 했다(예전엔 100%면 아무것도 안 찍혀 원본을 알 길이 없었다).
- [x] **제단 보스 4종 체력 목표 (4명·60초)** · **`ls_config.js` perBoss** · ⚠️ 실전 미검증 · `GLOBAL_POWER` ×1.8 기준
  - 강철거인 3650 · 이그니스 7000 · 건틀렛 6250 · 네더라이트 괴물 10400. 유도 과정은 `ls_config.js` 주석에 전부 남겼다.
    (`GLOBAL_POWER` ×1.8 의 **실효** 배수를 다시 계산하며 07-31 재역산한 값이다 — 명목 ×1.8 이 그대로
    화력이 되지 않는 건 성역 축복의 덧셈 항 +3.0 이 배수를 안 타기 때문이다.)
  - **이 넷은 `globalHp: 180` 을 안 탄다** — `absHp` 가 %를 덮는다. 의도된 분업이다: 제단 보스는 목표 시간 역산, 나머지 29종은 180% 비례 보정. 제단 보스에 180%를 못 쓰는 건 원본이 수백 HP라 ×1.8 해도 몇 초면 죽어서다.
  - 강철거인만 딜 유지율 0.45(나머지 0.6) — **패링형이라 못 때리는 구간이 길다.** 같은 0.6 을 쓰면 체력이 1/3 더 붙고 그 시간은 전부 «서 있는» 시간이 된다.
  - 건틀렛이 이그니스보다 낮은 건 방어도 8 때문이다(실측 DPS 는 방어도 0 표적 기준).
  - 보정: `새 체력 = 지금 체력 × (60 / 실제걸린초)` → `/bossdiff abs` 로 즉시 → `/bossdiff export`
8. ~~**유성 사격의 폭발이 0회다**~~ · **해결됨 (2026-08-04 밤)** · 원인만 미확정

   > ✅ **훅을 옮긴 뒤 실측: 유성 사격 18타 평균 9.9 → 99.3 / 103.0 (10배).**
   > 별빛 폭풍도 평균 56.6 → 77.4 / 85.5 로 살아났다. **이관이 통했다.**
   > 그 결과 시리우스가 78.9(꼴찌) → 지속 104.8(1위)로 튀어 폭발을 ×0.75 로 내렸다
   > (`DECISIONS.md` 1절 하단 · 지속 97.9 로 착지).
   >
   > **무적 프레임 훅만은 효과가 없었다.** 평타가 200타 → 197/199타, 평균 18.0 → 17.8/17.6 로
   > 그대로다. 초당 3.3발이 전부 집계되고 있으니 **애초에 무적에 막히지 않고 있었다** —
   > `onRelicArrowPierce` 주석이 설명하는 증상은 지금 존재하지 않는다. 훅은 무해하니 뒀다.
   >
   > **원인은 여전히 모른다.** 이동이 통했다는 건 그 경로가 실제로 안 돌았다는 강한 증거지만,
   > mixin 적용 후 바이트코드에 호출이 남아 있는 것과는 계속 충돌한다. 범인 모드도 못 찾았다.

   > ⚠️ **이 항목을 한때 「`ProjectileImpactEvent` 가 한 번도 안 불린다」로 단정해 적었다.
   > 물린다.** 훅에 로그를 박고 «로그 0» 을 봤다는 게 근거였는데, 그 뒤 **mixin 적용 후
   > 최종 바이트코드에 `EventHooks.onProjectileImpact` 호출이 멀쩡히 남아 있는 것**을 확인했다.
   > 게다가 그때 쓴 «손으로 소환한 화살» 시험이 못 믿을 물건이었다 — 같은 조건에서 6발을
   > 쏘고 표적 체력이 100 → 100 이었다(한 발도 안 맞았다).
   > **못 믿을 계측으로 내린 결론을 확정으로 적으면 그 거짓 확신이 문서에 굳는다.**

   **확실한 것:** 8종 실측에서 유성 사격이 「18타 평균 9.9」였다. 18타는 정확히 6시전 × 3발,
   9.9 는 화살 직격만의 값 — **폭발(5성 약 69)이 0회.** 시리우스 꼴찌(78.9)와 무관하지 않다.

   **범인 모드도 못 찾았다.** 화살·투사체 mixin 을 가진 모드 9개를 훑었지만(CerbonsAPI 멀티파트,
   Apotheosis 계열, Aether, supplementaries 등) 착탄 경로를 취소하는 것은 없었다.

   그 이벤트에 걸려 있던 넷은 어차피 **도는 게 증명된 이벤트**로 옮겼다(아래). 원인이 무엇이든
   폭발 0회는 실측이고, 옮겨서 나빠질 일은 없다:
   · `RelicEventHandlers.onProjectileImpact` — **유성 사격 폭발**
   · `RelicEventHandlers.onRelicArrowPierce` — **유물 화살의 무적 프레임 무시**
   · `RelicEventHandlers.onArrowSplash` — 별빛 폭풍 착탄 확산
   · `ExplorerGimmicks.onProjectileImpact` — 프로스트모 화살 경고 (07-31 작업)

   **→ 2026-08-04 넷 다 옮겼다. 단 ⚠️ 아직 도는 걸 못 봤다.**
   · 별빛 폭풍 확산 · 유성 사격 폭발 → `LivingIncomingDamageEvent` (맞았을 때 얹는다)
   · 유물 화살의 무적 프레임 무시 → `LivingDamageEvent.Post` (박힌 뒤에 걷는다 — 방향이 뒤집혔다)
   · 프로스트모 화살 경고 → `ExplorerGimmicks` 의 보스 틱에서 반경 1.5칸 화살 훑기
     (피해가 0이라 피해 이벤트가 아예 안 열린다 — 그게 알리려는 사실이다)

   **바뀐 동작 하나: 빗나간 화살은 안 터진다.** 피해 이벤트는 맞았을 때만 오기 때문이다.
   모든 화살을 매 틱 훑으면 살릴 수 있지만 비용이 얻는 것보다 크다고 봤다.

   ⚠️ **검증 필요 — `TEST-PLAN.md` 5-3 맨 위.** 내 합성 화살 시험은 못 쓴다(6발에 피해 0).
   **시리우스로 `/dummy` 60초**를 재서 「유성 사격 평균」이 9.9 → 50~70 대로 가는지 보면 갈린다.
   안 가면 이관이 실패한 것이고, 그때는 `onArrowHitEffects` 진입점에 로그를 박고 다시 본다 —
   **단, 이번엔 반드시 «실제로 쏜 화살»로 확인할 것.** 손으로 소환한 화살은 표적을 안 맞춘다.

   ※ **교훈: 「구현했다」와 「도는 걸 봤다」는 다르다.** 이 셋은 정교한 주석까지 달려 있었다.
     **그리고 이 항목 자체가 지금 같은 상태다** — 옮겨는 놨지만 아직 본 건 아니다.

- [x] ~~**시리우스 「유성 사격」이 평타보다 약하다**~~ · **해결됨 (2026-08-04)** — 원인은 위 D절 8번.
      폭발이 아예 안 터지고 있었다. 훅을 옮겨 살린 뒤 평균 9.9 → 99~103, 시리우스가 꼴찌에서
      1위로 뒤집혀 폭발을 12.71 → 9.53 (×0.75) 으로 내렸다. 지속 97.9 로 원거리 동료와 동급.
      `유성 사격 18타 평균 9.9` vs `평타 200타 평균 18.0` — **스킬 한 발이 평타 절반이다.**
      18번이나 썼는데 전체의 4%(3.0 DPS). 스킬이 평타보다 약하면 안 쓰는 게 이득이고,
      그러면 시리우스는 「우클릭만 누르는 활」이 된다. 꼴찌(78.9 · 평균 −11%)인 것보다 이게 크다.
      · `DECISIONS.md` 1절의 **솔라리스 문제(스킬이 DPS를 깎는다)와 같은 병**인데,
        정작 솔라리스는 이번에 97.0 으로 상위권이었다 — 병이 시리우스로 옮겨갔다.
      · 배율로는 못 고친다. 스킬 자체의 피해·캐스팅 시간 구조를 봐야 한다.
- [x] ~~**`calc_power_ratio.py` 를 실측 기반으로 재정비**~~ · **완료 (2026-08-05)** — 전면 재작성.
      옛 모델은 5성 실측에서 보정상수 k 를 역산하고 그 위에 `GLOBAL_POWER` 를 또 곱하는 구조라
      **±26% 를 예측했는데 실제는 ±11%** 였다(절반 이하로 틀렸다). 이제 모델링할 게 거의 없다 —
      8종 전부 실측값이 있어서 «실측 → 성급 환산 → 보스 체력» 세 단계뿐이다.
      성급 환산이 정비례인 이유(평타·스킬 둘 다 `ASC × GLOBAL` 을 탄다)를 머리말에 적어뒀다.
      · 같이 고친 것: **축복을 항상 켜진 것으로 보던 기준.** 관문 제단은 성역에서 2000~5000m
        밖이라 성소 축복(64칸)이 안 걸린다 — 보스 체력은 «축복 밖» 이 맞다.
      · `IN_GAME` 열을 넣어 인게임 값과의 차이를 항상 보여준다. 지금 네 관문 전부 +0%.
- [ ] **보스 공격력(dmg) 재검토** · 🟡 · 지금 전부 100(원본). 체력만 올리면 스펀지가 된다(이 파일 mob 섹션의 원칙과 어긋남). 성수별 체력 증가와 같이 봐야 해서 분리. 후보: T2 130 / T3 170 / T4 220
- [x] **T4 조합 기믹: 네더라이트 괴물 「용암의 심판」** · **구현: `MonstrosityGimmick.java`** · ⚠️ 실전 미검증
  - **새 어휘를 안 가르친다 — 시험이다.** 두 박자: 보라(사람마다 원, 흩어져라) → 2.5초 뒤 노랑(결계 하나, 뭉쳐라).
    두 어휘가 정확히 반대라서 하나라도 헷갈리면 두 번째에서 걸린다. 어려운 건 순서와 시간뿐이다.
  - 2페이즈(50%)는 그 위에 빨강(보스 발밑)을 얹는다. **세 어휘 동시 = 이 팩에서 가장 복잡한 순간이고 그 이상은 만들지 않는다.**
    넷을 겹치면 읽는 게 아니라 외우는 게 된다.
  - 2박을 `Telegraph` resolve 안에서 내지 않고 **다음 주기를 앞당겨** 낸다 — resolve 안의 재시전은 CME 다(위 함정 2).
  - 시험: `/lsgimmick monstrosity <summon|now>`
- [x] **최종 기믹: 리치** · **구현: `LichGimmick.java`** · ⚠️ 실전 미검증
  - 파랑(멈춰라) + 노랑. **여기만 진짜 무적을 우리가 만든다** — BOMD 리치는 `hurt()` 를 안 건드려서
    막을 규칙 자체가 없다(나머지 넷은 모드가 이미 가진 규칙에 색을 입힌 것이다). `LivingIncomingDamageEvent` 로 건다.
  - 삼킴 장부는 **만료 시각을 키로** 잡아 스스로 사라진다 — 보스가 중간에 죽어도 무적이 남지 않는다.
  - 시험: `/lsgimmick lich <summon|now>`
- [ ] 보스 그로기 게이지 · 🔴 · lsrelics 스킬 태그 (모드 보스 AI 를 못 건드려 보류)
- [x] **소프트 격노** · **구현: `ls_enrage.js`** — 전투 8분 유예 후 30초마다 보스 공격력 +10%, 상한 +200%. `/enrage status`
- [x] **공성 마지막 웨이브 미니보스 「균열의 선봉」 (2026-07-31)** · **구현: `SiegeVanguardGimmick.java` + `ls_siege.js`** · ⚠️ 실전 미검증
  - `cataclysm:ignited_berserker`, 마지막 웨이브에만. **빨강을 처음 보는 자리가 T1 관문이었다** —
    연습 없이 시험부터 보는 구조였다. 공성은 매주 오고 실패해도 관문 진행이 막히지 않으니 배우기 좋은 자리다.
  - 반경 4.5 · 예고 1.5초는 T1 과 **똑같이** 두고 피해만 10 → 6. 수치를 바꾸면 배운 게 T1 에서 안 통한다.
  - 함정: 태그 하나로 쓰면 `/lsgimmick clear` 가 **실전 공성 개체를 지운다.** `TAG`(실전·시험 공용)와
    `TEST_TAG`(시험 전용) 를 나눴고, `summon()` 이 둘 다 붙인다. clear 는 `TEST_TAG` 만 본다.
  - [ ] T4 체크포인트 자체는 보류 · 🟡🔴 (체크포인트는 최종 보스에만 현실적)
- [x] **탐험 보스 4종의 숨은 규칙에 신호 붙이기 (2026-07-31)** · **구현: `ExplorerGimmicks.java`** · ⚠️ 실전 미검증
  - 관문 보스 다섯은 **어휘를 가르치고**, 이 넷은 **모드가 이미 가진 규칙에 색을 입힐 뿐**이다.
    그래서 신호만 낸다(`signalOnly`) — 새 규칙을 더하지 않는다. 색도 의미가 맞는 초록(OPENING)만 쓴다.
  - javap 로 파냈다. 넷 다 **문서에도 설정에도 없고 인게임에 신호가 없다**:
    | 보스 | 규칙 | 우리가 붙인 신호 |
    |---|---|---|
    | 프로스트모 | 불 ×1.25 · **화살은 피해 0** | 화살 착탄 시 경고 |
    | 히드라 | **벌린 입만 온전한 피해, 나머지 1/8** · 20칸 밖 무피해 | 머리마다 초록 |
    | 유령기사 | 돌진 중이 아니면 **방어도 5배** | 돌진 중 초록 |
    | 우르가스트 | 발작 중 피해 1/10 | 액션바 (색 안 씀) |
  - **프로스트모 건이 제일 크다: 시리우스는 이 보스에게 아무것도 못 한다.** 별빛 사격·유성 사격·화살 폭풍이
    전부 진짜 `Arrow` 엔티티다. 강철거인은 등 뒤로 돌면 되지만 여기는 우회로가 없다 — 밸런스 결정 대상.
  - 리플렉션으로 읽는다(`hc[]`·`getCurrentMouthOpen()`·`damageUntilNextPhase` 등). 셋 다 비의존 모드라
    컴파일 의존을 걸 수 없다. 프로스트모만 `ProjectileImpactEvent` — `hurt()` 가 false 면 피해 이벤트가 안 열린다.

**스토리·건축 (리셋 후)**
- [ ] 별지기의 수기 12편(감정 아크) + 물리 기록의 방 · 🔴
      ※ **FTB Quests 는 2026-08-06 에 제거했다**(`MODS.md`). 「숨김 챕터 기록 보관소」의 자리는
        이제 **호데고스**(`ls_voice.js` — 3채널 연출 + 대사 테이블)이거나 **글이 적힌 실제 책**이다.
        전자가 이미 이 서버의 화자이므로 수기를 대사 테이블로 푸는 쪽이 자연스럽다.
- [ ] 오프닝 "별이 꺼지는 밤" 시퀀스 대본 · 🟡 · 첫 세션
- [x] **유물 8종 전승 2행 — 옛 별지기의 목소리 (2026-07-31)** · **구현: `ls_relic.js` `echo`** · ⚠️ 실전 미검증
  - `lore` 는 유물에 새겨진 명문이다 — 별이 사람에게 하는 말이라 웅장하다.
    `echo` 는 **이 무기를 마지막으로 쥐었던 사람**의 한 줄이다. 사람의 말이라 짧고, 문어체가 아니고, 자랑이 없다.
  - **여덟 줄 중 어느 것도 「우리는 실패했다」고 말하지 않는다.** 한 줄씩 보면 옛 지혜로 읽히고,
    모아 보면 아무도 돌아오지 못한 세대의 기록이 된다. 알아채는 건 읽는 사람 몫이다.
    스틱스(「나오는 법은 아무도 안 가르쳐 줬다」)와 게볼그(「던지면 돌아온다. 나는 그러지 못했다」)
    두 줄만 조금 더 나간다 — 그 둘이 「일부는 타락해 관문의 수문장이 되었다」로 이어지는 자리다.
  - 지급 시엔 명문 뒤 **3초 늦게** 나온다. 붙여 내보내면 두 줄이 같은 사람의 글로 읽혀
    「누가 썼나」라는 질문 자체가 안 생긴다. `/relic` 에서는 **가진 사람에게만** 보인다.
  - 정체 공개(T3~T4)는 여기서 하지 않는다. 심기만 한다 — `docs/STORY.md` 부록B 떡밥3.
- [ ] 공용 시각 기호 3종(8각 별/꺼진 등불/검은 덩굴) 건축 문법 · 🔴 · 리소스/건축
- [ ] 최종문 실루엣(어둠의 첨탑) · 🔴 · 스트럭처

**운영 정책 (코드 아님 — E절 결정 필요)**
- [ ] 주간 코어 세션 선언 · 챕터 릴리스 캘린더 · 첫 30분 온보딩 · 자동 백업 스케줄 확인

**서버 위생 — 열어두면 물리는 것들**

> 출처는 `상용화_비교_리뷰_2026-07-24.md` 4절이었다. 그 문서는 2026-07-31 에 지웠는데,
> **일주일이 지나도록 이 넷이 그대로였다.** 아무도 안 읽는 문서에 적혀 있었기 때문이다.
> 그래서 여기로 옮긴다 — 지적이 아니라 **위치가 문제였다.**

- [~] ~~**화이트리스트가 꺼져 있다**~~ · **일부러 꺼둔 것 (유저 결정 2026-07-31)** — 미결 아님.
      `white-list=false` · `whitelist.json=[]` · `enforce-whitelist=false` 는 의도된 상태다.
      지금 막아주고 있는 건 `online-mode=true` 다(정품 계정만 접속 가능).
      · **다시 열어야 하는 시점: 이 PC 를 외부에 개방하거나 포트포워딩할 때.**
        그때는 RCON 25575(E절 7)와 **같이** 본다 — 둘 다 「지금은 로컬이니까」라는 같은 전제에
        기대고 있어서, 개방하는 날 둘이 동시에 문제가 된다.
      · ※ 이 줄을 지우지 말 것. 2026-07-24 리뷰가 이걸 「문제점」으로 올렸고, 07-31 에 내가
        그 지적을 그대로 TODO 로 옮겼다 — **닫힌 결정이 안 적혀 있어서 두 번 열린 것이다.**
        제련 배율(E절 6)이 세 번 열린 것과 같은 구조다.
- [x] ~~**제거된 모드의 잔재가 로그를 오염**~~ · **조사 완료 (2026-08-06)** — `tools/check_log.py` 로 닫았다.
  - **제목이 틀렸다. 「제거된 모드의 잔재」가 아니다.** 모드 jar 를 직접 열어 확인했더니
    지목된 모드가 **전부 설치돼 있었다**(DungeonsArise · dungeons-and-taverns · FarmersDelight · Cataclysm).
    실제 원인은 넷 다 다르고, 하나도 우리 것이 아니다:

    | 증상 | 진짜 원인 |
    |---|---|
    | 도전과제 4개 로드 실패 | **부모가 존재하지 않는다.** `dungeons_arise:find_small_prairie_house` 는 jar 안에 없고, `minecraft:root` 는 바닐라에 없다(`story/root`·`nether/root`…). 둘 다 그 모드의 버그 |
    | `melon_juice` 레시피 2개 | **의도된 제거다.** expandeddelight 가 `{"neoforge:conditions":[{"type":"neoforge:never"}]}` 로 덮어쓴다 — NeoForge 표준 관용구. 레시피는 정상적으로 빠지고 경고만 난다 |
    | cataclysm 블록 태그 누락 | Cataclysm 이 자기 태그를 참조하면서 태그 파일을 안 실었다 |
    | 믹스인 WARN 72줄 | 55는 refmap 부재(배포 jar 는 원래 안 싣는다), 나머지는 **클라 전용 클래스**와 안 깐 모드용 선택적 대상 |

  - **잃는 것: 장식 도전과제 4개뿐이다.** 그중 둘은 보상이 `nova_structures:choose_wander` 인데
    그 모드를 안 깔았으므로 고쳐도 의미가 없다. **남의 모드 파일을 덮어써 고치지 않는다** —
    그쪽이 업데이트되면 우리 덮어쓰기가 조용히 남는다.
  - **진짜 문제였던 「노이즈가 진짜 오류를 가린다」는 `tools/check_log.py` 로 닫았다.**
    로그를 지우거나 필터링하지 않는다(지우면 필요할 때도 없다). 대신 「지금 무엇이 나오는 게
    정상인가」를 기준선 16종으로 적어 두고 **그 밖의 것만** 보여준다 — `ARCHITECTURE.md` 함정
    검출의 기준선과 같은 방식이다. 지금 104줄이 걸리고 **새로운 줄 0**.
    `lsWarn`(우리 코드의 경고)은 **기준선을 아예 안 탄다** — 재울 것이 아니라 고칠 것이다.

    ```bash
    py tools/check_log.py          # 새 문제만
    py tools/check_log.py --all    # 기준선 16종과 «왜 양성인가»
    ```

- [ ] **경제 싱크가 없다** · 🟡 · 유입은 여럿(카지노·현상금·공성 보상)인데 상시 대형 싱크가 없다.
      마을 발전이 끝나면 Ducat 이 갈 곳을 잃는다 — 주간 별빛 금고(B절)가 그 자리 후보다.
      ※ **이건 서버 위생이 아니라 콘텐츠 설계다.** B절 「주간 별빛 금고」와 같은 항목이고,
        그쪽을 만들면 여기도 같이 닫힌다. 여기서 따로 손댈 것이 없어 위치만 남겨 둔다.
      ※ **에메랄드 쪽은 2026-08-08 에 반쯤 닫혔다.** 균열 3종이 확정 보상으로 에메랄드를 20~43개씩
        뿌리고 있었는데, 도박장 칩이 **에메랄드 실물**이라 반복 제작 가능한 열쇠가 판돈을 찍어내는
        구조였다(황금은 순 +34). 전부 자수정·다이아로 갈아치웠다. Ducat 싱크는 그대로 남아 있다.
- [x] ~~**`neoforge-21.1.241-installer.jar` + `.log` 가 서버 폴더에 잔류**~~ · **완료 (2026-08-06)** —
      지웠다(7.0MB + 0.5MB). 설치 결과물은 `server/libraries/net/neoforged/neoforge/21.1.241/` 에 남아 있고,
      인스톨러가 다시 필요하면 받으면 된다. `SETUP.md` Phase 4-2 에 그렇게 적어뒀다.

---

## D. 코드를 훑다 발견한 이슈 (조사와 별개 — 확인/수정 권장)

> ### ⚠ KubeJS(Rhino) 함정 — 앞으로 스크립트 쓸 때 반드시 지킬 것
>
> **1. `try` 블록 안에서 `const`/`let` 을 쓰지 마라.** `var` 로 쓰거나 `try` 밖에서 선언한다.
> Rhino 가 `InternalError: TypeError: redeclaration of var X` 로 터진다. 특히 **같은 이름을
> 다른 `try` 블록에서 또 선언하면 확실히 터진다** (`try` 안의 선언이 상위 스코프로 누출된다).
> `var` 는 재선언이 합법이라 안전하다.
> · 실제 사례: `ls_voice.js` 의 접속 핸들러와 tick 핸들러가 `st`/`rf`/`day` 를 둘 다 선언 →
>   접속 인사·재접속 요약이 통째로 죽어 있었다. `ls_siege.js` 재타겟팅 2곳도 같은 상태였다.
> · **문법 검사(`node --check`)로는 절대 안 잡힌다** — 유효한 JS 라서 런타임에만 터진다.
>   서버 로그의 `[KubeJS Server]` 줄을 반드시 확인해야 한다.
>
> **2. 서버 스크립트는 전역 스코프를 공유한다.** 파일이 달라도 최상위 `function`/`const` 이름이
> 겹치면 나중에 로드된 쪽이 이긴다. 새 파일은 고유 접두사를 붙인다(`vc*`, `tw*`, `sf*`, `rf*`).
>
> **3. `runCommandSilent()` 의 반환값을 쓰지 마라 — 없다.** 시그니처가 `void` 다:
> `public default void kjs$runCommandSilent(java.lang.String);`
> 그래서 아래 관용구는 **처음부터 작동한 적이 없다.** 항상 `undefined` 가 들어갔다:
> ```js
> have = server.runCommandSilent(`clear ${name} ${item} 0`)   // ✗ 개수가 안 온다
> ```
> JS 에서 `undefined`·`NaN` 은 **모든 비교가 false** 라 부족 검사를 그대로 통과하고, 증상이 둘로 갈렸다:
> · 값이 저장까지 흘러가면 → `Cannot convert NaN to int` 로 **명령이 통째로 죽는다**(빨간 오류)
> · 비용 검사면 → **공짜로 통과한다** (정수 없이 유물 수령·관문 개봉·칩 없이 베팅)
> 5개 파일 전부 이 상태였다: `ls_town`·`ls_bounty`(크래시) · `ls_relic`·`ls_rift`·`ls_casino`(공짜 통과).
> **→ 개수는 인벤토리를 직접 읽는다. `ls_util.js` 의 `lsCountItem(player, id)` / `lsTakeItem(player, id, n)` 사용.**
> 저장 헬퍼도 `twInt()` 처럼 한 번 더 거르는 게 안전하다.
>
> **4. `kjs$` 접두사를 붙여 부르지 마라.** 자바 쪽 실제 이름이 `kjs$setMaxHealth` 여도
> 스크립트에서는 `setMaxHealth` 로 부른다. 붙이면 `Cannot find function` 으로 핸들러 전체가 죽는다.
> · 실제 사례: `ls_bossdiff.js` 가 `e.kjs$setMaxHealth()` 를 불러 **보스 난이도 조절이 통째로
>   무효**였다(체력·공격력 둘 다). 몹 스폰마다 에러가 찍히고 있었다.


> 조사 후보가 아니라, 코드를 읽으며 눈에 띈 것들. 착수 전 실제 인게임에서 재현 확인 권장.

1. ~~**`ls_siege.js` — `const accrued` 재할당**~~ · **해결됨** (A절 A6). `dawnReward` 를 따로 두는 방식으로 고쳤고, 채팅/로그가 정규화 전 값을 찍던 표시 버그도 같이 잡았다. 아래는 당시 기록.
   `finishSiege`에서 `const accrued = sfGetI(...)`(≈389행)로 선언한 뒤, `dawn` 분기(≈419행)에서
   `accrued = Math.round(accrued * REWARD_MULT)`로 **재할당**한다. `dawn` 경로는 "밤을 다 못 깨고 아침이 온"
   흔한 상황(`finishSiege(server,'dawn')`, 619행)이라 실제로 실행된다. KubeJS(Rhino)에서 const 재할당은
   런타임 TypeError를 던질 수 있음 → `let accrued`로 바꾸거나 새 변수 사용. **한 줄 수정, 우선 확인 권장.**

2. ~~**`ls_beacon.js` — 정화 대상 목록 이중 관리**~~ · **해결됨 (2026-07-31)**. `PB_PURGE` 배열은 «문서용 원본»으로 남아 있었을 뿐 - 쓰는 곳이 없었다 - . 지웠고 이제 태그 `#last_stardust:purge` 하나가 유일한 출처다. 이걸 놓친 게 `tools/scan_dead_kubejs.py` 가 함수만 보고 상수는 안 봤기 때문이라, 스캐너도 같이 고쳤다. 아래는 당시 기록.
   실제 정화는 태그 `#last_stardust:purge`(`purge.json`)를 쓰고, 코드의 `PB_PURGE` 배열은 "문서용 원본"이다
   (주석에 명시됨). 대상 변경 시 **두 곳을 같이** 고쳐야 함 — 잊으면 조용히 어긋난다. 한쪽을 SSOT로 정리 검토.

3. ~~**복구 커맨드 비대칭**~~ · **해결됨 (2026-07-31)**. 다만 **이 항목이 지목한 둘은 애초에 있었다** —
   `/relic revoke`·`/ascend set` 은 진작 구현돼 있었고 이 줄이 낡은 기록이었다. 명령을 전수로 뽑아
   비교하고 나서야 진짜 구멍이 드러났다: `/enrage` 는 **끄는 방법도, 8분을 안 기다리고 시험하는 방법도 없었다.**
   → `/enrage clear`·`/enrage now <n>` 추가(`ls_enrage.js`). 마을은 `/town level <track> <n>` 이 이미 있다.
   ※ **교훈: 「없다」고 적힌 문서를 믿고 고치러 가면, 있는 걸 또 만들고 없는 건 계속 없다.**
   이 절의 다른 줄들도 같은 나이다 — 착수 전에 실제 코드로 한 번 확인할 것.

4. ~~**`main.js` 예제 잔재**~~ · **해결됨 (2026-07-31)**. `server_scripts/main.js`·`startup_scripts/main.js` 삭제. 기동 로그에서 `Hello, World` 가 사라졌고 스크립트 수가 23 → 22 가 됐다.

7. ~~**몹 스케일링이 서버 재시작마다 복리로 붙었다**~~ · **해결됨 (2026-07-31)** · 🔴 **이번에 찾은 것 중 제일 컸다**
   `EntityEvents.spawned` 는 «새로 태어날 때»만 오는 게 아니라 **디스크에서 다시 로드될 때도 온다.**
   그래서 껐다 켤 때마다 살아 있던 몹 전부가 다시 곱해졌다. **실측: 좀비 하나가 재시작 한 번에
   20 → 34.56 (20×1.2³).** 재시작 10번이면 ×6.2 다. 백업·업데이트 때마다 조용히 쌓여 왔고 오류는 없었다.
   → 세 번 시도해서 고쳤다. 앞의 둘이 왜 실패했는지가 더 중요하다:
   · **엔티티 태그 표식** — 재로드 때 이 이벤트가 **태그가 붙기 전에** 온다. 그 시점 `e.tags` 는 비어 있다.
   · **이름 붙은 모디파이어**(`ls_enrage.js` 방식) — 소환 직후엔 맞는데, **재시작하면 모디파이어가
     base 에 구워진다.** 구워진 값이 다음 로드의 새 base 가 되어 같은 복리가 된다.
   · **성공한 것: 현재값을 아예 안 읽는다.** `LS.defaultAttrBase(e, id)` 가 그 «몹 종류의 공장 출고값»을
     주고, `출고값 × 배율` 은 몇 번을 돌려도 같다. 탐지가 필요 없어졌고, **이미 망가진 개체도 다음
     로드에서 제 값으로 돌아온다.**
   ※ **교훈: 「한 번만 실행」을 표식으로 보장하려 하지 말고, 「여러 번 해도 같은 결과」로 만들어라.**
     로드 순서에 기대는 표식은 언젠가 순서가 바뀐다.
   · **이미 부푼 개체는 `/mobscale fix`** — 자동 복구에 구멍이 셋 있다: ①티어 0이면 훅이 일찍
     return ②**늑대·철골렘·벌은 이제 비적대라 훅이 아예 안 본다 — 이 명령으로만 돌아온다**
     ③청크가 로드돼야 훅이 온다. 07-31 시험 서버에서 **로드된 67기 중 30기가 부풀어 있었다.**
     멱등이라 여러 번 돌려도 안전하다(두 번째부터 「0기」).

6. ~~**격노 알림이 보스 수만큼 방송된다**~~ · **해결됨 (2026-07-31)**. 「보스 공격력 +N%」라는 **전역 서술**을
   개체 루프 안에서 `server.tell` 하고 있었다. 시험 서버 로그에 **같은 줄이 한 초에 다섯 번** — 시험 소환한
   다섯 기가 나란히 8분 유예를 넘긴 것이다. 실전에서도 공성 마지막 웨이브·T4 처럼 보스가 겹치면 그대로 겹친다.
   → 루프는 `enBegan`/`enLoud` 두 값만 적고 방송은 루프 뒤 한 번. 둘 다 걸리면 두 줄이 맞다(시작 ≠ 누적).
   ※ 찾은 경로가 코드 읽기가 아니라 **로그 읽기**였다. 종료된 서버의 출력 파일을 확인하다 나왔다.

8. ~~**공성 종료가 자기 자신을 다시 부르고 있었다**~~ · **해결됨 (2026-08-07)** · 🔴 **넷 중 제일 컸다**
   `finishSiege` 가 `kill @e[tag=ls_siege]` 를 **`LS.endSiege` 보다 먼저** 부르고 있었다. `kill` 은
   `EntityEvents.death` 를 **동기로** 발생시키는데, 그 시점엔 `siegeActive` 가 아직 true 라 핸들러의
   `!LS.siegeActive` 가드를 그대로 통과했다. 그래서 청소로 죽는 몹마다 `siegeRemaining` 이 깎이고,
   0 이 되는 순간 `onWaveCleared` 가 불렸다:
   · 웨이브가 남아 있으면 → **끝나는 공성에 새 웨이브를 소환**(공성은 곧 꺼지므로 유령 몹이 남는다)
   · 마지막 웨이브였으면 → `finishSiege` 재귀 → **`dawn` 이 `win` 으로 중첩 승격**되고 보상이 두 번
   「새벽에 몹이 남은 채 끝나는 밤」은 이 서버의 평범한 밤이라 **상시 발생**했고, 예외는 안 났다.
   → `LS.endSiege` 를 `kill` **앞**으로. `cancelSiege` 에도 같은 순서 버그가 있어 같이 고쳤다.
   ※ **교훈: 조건을 하나 더 다는 대신 순서를 고쳤다.** 가드는 이미 있었고 제 일을 못 하는 위치에 있었을 뿐이다.

9. ~~**성벽이 부서져도 결과가 안 바뀌었다**~~ · **해결됨 (2026-08-07)**
   `WALL_BASE_HP 3000` 과 수리비(완전 수리 = 300 Ducat + 원목 75 + 철괴 30 + 조약돌 150)로 판돈을
   만들어 놓고 **`finishSiege` 의 `dawn` 분기가 `wallHp` 도 `wallBroken` 도 안 읽었다.** 성벽을 잃든
   말든 "성역은 밤을 버텨냈습니다"가 똑같이 나갔다.
   → `dawn` + 성벽 붕괴 = 새 결과 **`lose`**: 위협도 +1 · 그 밤의 누적 보상 몰수 · 희망 -1.
   · 금고를 추가로 깎지는 **않는다** — 수리비가 이미 페널티다. 두 번 물리면 「졌는데 재기까지 막히는」 밤이 된다.
   · 웨이브를 전부 지운 `win` 은 성벽이 무너졌어도 그대로 승리다(다 잡았으면 이긴 것이다).
   · **첫 공세만은 `lose` 여도 유물이 해금된다** — 해금이 여기 걸려 있어 밀리면 안 되기 때문(원래 주석의 의도).

10. ~~**`/mobscale` 이 「비적대 몹 제외」라고 거짓말하고 있었다**~~ · **해결됨 (2026-08-07)** · 위 8번 항목의 후속
    실제 기준은 «MobCategory 가 monster 인가»다. 그래서 **덤비는 늑대·북극곰은 안 세지고**, 반대로
    **분류만 monster 인 남의 모드 엔티티는 세졌다.** 실측으로 걸린 것: `krip_turrets` 포탑 8종이
    `MobCategory.MONSTER` 로 등록돼 있어 **플레이어가 설치한 자기 포탑이 관문 진행에 따라 같이 강해졌다**
    (`/mobscale set 4` 기준 HP 10 → 22, 공격력 45 → 65). 「세상이 험해진다」는 이 파일의 목적과 정반대로,
    진행할수록 **내 방어가 공짜로 세지는** 통로였다.
    → `MS_NEVER_SCALE` 접두사 목록으로 제외 + 명령어 문구를 실제 규칙대로 고쳤다.
    · **`msIsHostile` 한 곳만 고치면 된다** — `spawned` 훅과 `/mobscale fix` 가 둘 다 이 함수를 부른다.
      목록을 양쪽에 두면 언젠가 갈리고, 갈린 자리는 조용하다.
    · 검증: 티어 4 에서 포탑 10/45(제외) · 좀비 44(=20×2.2, 정상 적용).
    · 이 항목으로 아래 **E절 8번**(2026-07-25 부터 열려 있던 「적대 판별 범위」)이 닫힌다.

11. ~~**되돌릴 수 없는 진행에 인원 조건이 없었다**~~ · **해결됨 (2026-08-07)**
    `partyCount` 가 `Math.max(1, n)` 이라 **0 명이 1 명으로 세어진다.** 웨이브 크기 계산에는 맞는 바닥인데,
    그걸로 「아무도 없다」를 판정하던 자리가 있었다. 결과로 무인 상태에서:
    · **첫 공세가 소비된다** — `firstSiegeCleared` 가 정수를 나눠줄 사람이 없는 채로 플래그만 세운다. 다음 공세는 3일 뒤다.
    · **최종장 밤이 그냥 지나간다** · **최종 보스가 빈 세계에 소환된다** — `setTimeLocked` 로 **하늘까지 멈춘 채** 남는다.
    → `activePlayers(server)`(바닥 없음) 추가. 유물 해금·최종장 밤 통과·보스 소환 세 곳을 게이트.
    · 가드를 `spawnFinalBoss` **안**에 뒀다 — 호출부가 둘(밤 시작·보스 유실 재소환)이라 한쪽만 막으면 샌다.
    · **일반 공성 시작은 일부러 막지 않았다.** 막으면 「접속을 안 하면 공성이 안 온다」가 되어
      위협도만 쌓이는 다른 문제로 옮겨 갈 뿐이다. 결석 페널티는 별건으로 남긴다.

12. ~~**균열 3종이 도박장 칩을 찍어내고 있었다**~~ · **해결됨 (2026-08-08)**
    유저가 일반·정예·황금을 다 돌려 전리품을 모아 와서 숫자를 대조하다 나왔다.
    · 세 균열이 확정 보상으로 **에메랄드를 20 · 12 · 43개**씩 준다. 그런데 `ls_casino.js` 의
      칩이 **에메랄드 실물**이고, 열쇠는 반복 제작 가능하다 → 황금은 재료 에메랄드블록 1(=9)을
      먹고 43을 돌려줘 **순 +34**. 반복 콘텐츠가 판돈을 찍으면 카지노가 의미를 잃는다.
      → 전부 자수정(열쇠 재료)·다이아로 교체.
    · **일반 균열 한 번이면 정예 현상금이 끝났다.** 일반 구성에 마녀·변명자·소환사가 있었는데
      셋 다 `BT_ELITE` 다. 열쇠 원가가 파편 1인데 정예 현상금 보상이 파편 1 + Ducat 165~210 이라
      **파편이 순증**했다. → 일반은 사냥 몹만(2.1~2.4회로 균등), 정예 6종은 정예 균열이 커버.
    · 일반 전리품표가 `simple_dungeon` **×8** 이었다 — 3웨이브 싸우고 뼈 47·자갈을 받는 구성.
      롤을 3으로 줄이고 확정 보상을 올렸다.
    ※ **이미 게시된 현상금은 목록을 고쳐도 안 바뀐다**(뽑힌 값이 장부에 있다). `/bounty reroll` 필요.

13. ~~**별먼지가 나오는 곳이 없었다**~~ · **해결됨 (2026-08-08)**
    일반 열쇠 재료를 별먼지로 바꿔 놓고 드롭을 안 만들어서 **일반 균열이 실제로는 못 열렸다.**
    아이템 툴팁의 「구조물 상자에서 발견된다」도 그동안 거짓말이었다.
    → `lsrelics` 에 Global Loot Modifier. **KubeJS 2101 에는 전리품 «수정» 이벤트가 없다**(1.21 에서
    빠졌다) — 상자 전리품표가 566개라 덮어쓰기는 불가능하고 GLM 만이 기존 표를 안 건드린다.
    등급은 표 이름 규칙으로 자동 판정(대형 87 · 중형 364 · 소형 115). 실측 30/30 · 16/30 · 6/30.
    ※ 시험 중 한참 헤맸는데 원인이 `forceload add` 인자였다 — 그건 **블록 좌표**인데 청크 좌표로 넣어서,
      로드 안 된 청크에서 재고 있었다. 「드롭은 됐는데 없다」로 보였다.

14. ~~**원거리 4종의 발사 연출이 조준선 한복판을 덮었다**~~ · **해결됨 (2026-08-09, 유저 제보)**
    화살·별빛 탄·태양탄·힐 줄기가 전부 `eye + look*0.5` 에서 나갔다. 1인칭에서 그 점은
    **정확히 십자선 자리**라, 발사 파티클과 비행 궤적이 보려는 것을 그대로 가렸다.
    초당 2~6발 나가는 무기(별빛 탄 2.5발 · 태양탄 연사 6발)와 힐 줄기에서 특히 심했다.
    → `RelicSkills.muzzle(player, look)` — 눈보다 0.30 아래, 주손 쪽으로 0.22, 앞으로 0.60.
    · **옆으로 비킨 만큼은 `muzzleDir` 이 갚는다.** 그냥 평행 이동시키면 쏜 것이 항상 0.22칸
      옆으로 가는데, 스코프 저격은 정확도가 정체성이라 그 어긋남이 그대로 미스가 된다.
      조준선 위 24칸 지점으로 수렴시켜 없앴다(3칸에서 0.19 · 40칸에서 0.15 — 히트박스 0.6 안).
    · 적용: `StarBow`(평타) · `SolarMusket`(평타·스코프·연사) · `PanaceaStaff`(힐 줄기·신성 탄환) ·
      `RelicSkills.magicBolt`(별빛 탄) · `meteorShot`(화살 3발 + 마무리 섬광) · `rapidFire`(자기 버프 연출)
    · **투창(`JavelinManager`)은 원래부터 `eye − 0.2` 였다** — 이번에 안 건드렸다.

15. ~~**미리보기 도구가 면 텍스처를 서로 바꿔서 그리고 있었다**~~ · **해결됨 (2026-08-09)**
    `tools/render_model.py` 의 `FACES` 표에서 `north↔west` · `south↔east` 가 뒤바뀌어 있었다.
    `corners()` 의 비트가 (1=x, 2=z, 4=y) 인데 `'north'` 인덱스 `(0,2,6,4)` 는 **x 가 고정된 면**
    이다 — 마크에서 north 는 −Z 면이다.
    · **여섯 면이 다 있는 보통 상자에서는 티가 안 났다.** 형태는 정확하고 «어느 면의 텍스처가
      어디 붙느냐»만 어긋나서, 모델을 처음 보는 사람은 그게 원래 그림인 줄 안다.
    · **두께 0인 평면에서 정체가 드러났다.** 헤스페로스 el14~18(`from.x == to.x`)은 인게임에서
      east/west 두 면만 보이는데, 표가 뒤집혀 있어 렌더러는 «안 보여야 할» north/south 를
      큼직하게 그렸다. 그 면들의 UV 는 블록벤치가 남긴 쓰레기 값(`[0,0,0,x]` = 텍스처 0번 열)
      이라 **인게임에 없는 얼룩덜룩한 띠**가 날 뒤쪽에 나타났다. 리라 바닥의 흰 반점도 같은 원인.
    · ⚠️ **이 도구로 만든 이전 미리보기·판단은 전부 다시 봐야 한다.** 모델 채택·색 조정을
      이 그림으로 결정했다면 근거가 흔들린다(`docs/ASSETS.md` §9-A ③).

5. **전역 심볼 결합** · 여러 스크립트가 파일 경계를 넘어 전역(`ttGrant`, `asStamp`, `BOSS_SET`, `LS_CONFIG`)을
   공유한다. 의도된 설계(공유 스코프)이고 잘 동작하지만, **로드 순서·오타에 취약**하다. 신규 스크립트 추가 시
   `priority` 주석 규칙(현재 `ls_config`=1000)을 문서로 고정해 두면 안전.

6. **플레이어 애니메이션 — 첫 자세를 깔았다** (2026-08-09, 해결됨 / 확장 여지 있음) ·
   X「일도양단」의 «치켜듦»을 `player-animation-lib` 로 구현했다
   (`client/anim/SunderAnimation.java` + `RelicAnimations` + `RelicAnimPayload`).
   · 키프레임 **파일을 쓰지 않았다** — 팔 두 짝과 상체 회전뿐이라 블록벤치 파이프라인을 여는 게
     그림값보다 비쌌다. 값이 코드에 있어 인게임에서 보고 숫자만 고치면 된다.
   · **다음에 자세가 하나 더 필요해지면 그때 파일로 옮기는 게 맞다.** 코드로 굽는 방식은
     둘까지는 싸고 셋부터 비싸다.
   · `RelicAnimPayload.anim` 이 정수라 새 자세는 번호만 늘리면 된다.
   · ⚠️ 애니메이션 길이(16틱)와 서버의 `ParryManager.SUNDER_WINDUP` 이 **따로 적혀 있다.**
     한쪽만 고치면 그림과 피해가 어긋나는데 컴파일러가 안 잡아준다.

## E. 유저 결정이 필요한 항목 (내가 못 정하는 것)

> **➜ 선택지와 결과 수치는 `docs/DECISIONS.md` 에 계산해 뒀다.** 거기서 고르면 된다.
> 이 절은 «무엇이 열려 있나»의 목록이고, 그쪽은 «고르면 어떻게 되나»의 계산이다.
> 아래 6·7 번은 **2026-07-25 에 이미 닫혔다** — 닫힌 채로 둔다(그 표 참고).

착수 전에 **당신의 선택**이 필요한, 방향을 가르는 결정들:

1. **밤의 성격** — 밤을 "방어 농성"으로 유지할지, "밖에 나갈 이유(밤 보상)"를 넣어 이원화할지. (A절 첫 항목의 전제)
2. **보스 레이어 투자 범위** — 커스텀 기믹/그로기(🔴, lsrelics 개조)까지 갈지, 부활 규칙 정도로 그칠지.
3. **주간 코어 세션 도입 여부** — 스토리를 주 1회 정족수 세션에 잠글지(라이트 유저 보호 vs 자유도). RESEARCH 6 최상위 권장이지만 그룹 성향 문제.
4. **FTB Quests — 모드는 뺐지만 내용은 남아 있다** (2026-08-06)
   모드 다섯을 제거했다(`MODS.md`). 그런데 **`config/ftbquests/` 에 5챕터 25퀘스트 +
   한국어 설명 70줄이 그대로 있다**(성역의 불씨·변경 개척·지역 정화·관문 원정·최후의 관문).
   jar 만 다시 받으면 책이 그대로 살아난다 — **그 폴더는 지우지 말 것.**
   지금은 **바닐라 도전과제 27종**(기록)과 **호데고스**(서사)가 그 자리를 맡는다.
   → 남은 결정: 그 25퀘스트를 **되살릴지**, 아니면 **도전과제·호데고스로 흡수할지.**
     C절의 게이트였던 「착수 시점」은 어느 쪽이든 사라졌다.
5. **작업 순서 확정** — 위 A절(P0)부터 순서대로 갈지, 특정 시스템(예: 호데고스 목소리)을 먼저 당길지.

> **추천 시작점:** A절 6개(전부 🟢, 반나절 규모)를 먼저 쳐서 "손맛"을 체감한 뒤, B절에서 **호데고스 대사 테이블**을
> 첫 중간 작업으로 잡는 것 — 그게 STORY를 살아있게 만드는 단일 최대 레버다.

### 2026-07-25 검증 중에 올라온 결정 (완결)

> **6 → 그대로 유지. 7 → 방화벽 규칙 넣지 않음.** (유저 결정 2026-07-25)
> 6은 "주석의 의도와 어긋난다"는 지적을 알고도 현재 값을 택한 것이니, 나중에 채산성이 문제되면
> 이 결정을 다시 열면 된다 — 버그가 아니라 선택이다.
> 7은 지금 로컬/사설망 전용이라는 전제에 기댄 것이다. **이 PC를 외부에 개방하는 시점에 다시 봐야 한다.**

8. ~~**몹 스케일링의 "적대" 판별 범위**~~ · **닫힘 (2026-08-07)** — 두 걸음으로 닫혔다.
   ① 2026-07-31: `LS.isMonster()` 다리를 놓아 **MobCategory == monster 로 좁혔다**(유저 결정, DECISIONS 2절 C안).
      아래가 걱정한 「플레이어가 만든 철골렘」은 골렘이 `MISC` 라 이때 자동으로 빠졌다.
      대신 늑대·북극곰도 빠졌다 — 규칙이 한 문장으로 설명되는 쪽(«몬스터만 세진다»)을 알고 고른 값이다.
   ② 2026-08-07: 남은 구멍은 **분류를 남이 정한다**는 것이었다. `krip_turrets` 포탑이 `MONSTER` 로
      등록돼 있어 플레이어 소유물이 계속 세지고 있었다 → `MS_NEVER_SCALE` 로 제외(위 D절 10번).
   아래는 2026-07-25 당시의 원래 기록.
   `ls_mobscale.js msIsHostile()` 은
   **공격력 속성이 있으면 적대**로 본다. 원래는 `MobCategory == monster` 를 1순위로 보려 했지만
   그 코드는 `e.getType()` 이 id 문자열이라 처음부터 예외만 던지고 있었다(죽은 경로 — 제거함).
   결과적으로 판별이 의도보다 **넓다**: 철골렘·늑대·벌·눈사람도 스케일링을 받는다.
   · 여태 이 상태로 돌아왔으므로 **동작은 바꾸지 않고 그대로 뒀다.**
   · 좁히려면 MobCategory 를 제대로 읽는 경로를 찾아야 한다(KubeJS API 확인 필요).
   · 다만 늑대·북극곰처럼 "실제로 덤비는 중립 몹"은 세지는 게 맞을 수도 있다 — **의도를 정해야
     구현이 정해지는 항목**이다. 플레이어가 만든 철골렘이 세지는 것만은 아마 원치 않을 것이다.

6. **제련·수확 보너스 배율.** `TownEffects.BONUS_WEIGHTS = {33,24,24,19}` → 기댓값 **+1.29**,
   즉 광물 산출 **2.29배**. 그런데 같은 파일 주석은 *"무조건 +1이면 2배가 되어 채산성이 흔들린다"* 며
   2배를 거부하고 있다 — **스스로 거부한 것보다 센 값이 들어가 있다.** 33% 꽝이 섞여 체감만 보너스처럼
   느껴질 뿐이다. 여태 문제가 없어 보인 건 스택 단위로 1회만 추첨하던 버그가 값을 가려줬기 때문이고,
   그 버그를 고친 지금은 배율이 제대로 나온다.
   · 후보: `{50,30,15,5}` → +0.75 (1.75배) · `{60,30,8,2}` → +0.52 (1.52배)
   · **그대로 두는 것도 선택이다.** 공방 Lv1이 초반 트랙이라 체감이 크다는 점만 알고 정하면 된다.
7. **RCON 포트 노출.** 정상 종료·원격 점검을 위해 `enable-rcon=true` (25575) 를 켰다. 마인크래프트
   바닐라에는 바인드 주소 설정이 없어 **`0.0.0.0` 고정**이다. 지금은 로컬/사설망뿐이지만, 이 PC를
   친구들에게 개방하거나 포트포워딩할 때 **25575 를 반드시 막아야 한다** — RCON 은 인증만 통과하면
   서버 콘솔 전권이다. 인바운드 차단 규칙을 미리 넣어둘지 결정 필요.
