# Last Stardust — 3번: 적용 검토 (코드·모드 대비 갭 분석)

> 입력: `docs/RESEARCH.md`(1번 조사)의 **적용 후보 표** + `docs/STORY.md`(2번 스토리)의 **연출 재료**.
> 대상: 현재 서버에 실제로 구현된 것 — KubeJS 21개 스크립트(`server/kubejs/server_scripts/`),
> 커스텀 모드 `lsrelics`(8유물), 모드 90여 종(`Mod/`).
> 방식: 조사 보고서의 후보를 하나씩 **현 구현과 대조**해 상태를 판정하고, 추가/수정 지점을 적었다.
> 판정: ✅ 이미 구현 · 🟡 부분 구현(뼈대는 있으나 조사 후보의 핵심이 빠짐) · 🔲 미구현.
> **모든 항목은 "후보"다** — 채택 여부는 유저 검토 후 결정. 실행 우선순위는 `docs/TODO.md`(4번)에 정리했다.

> ## ⚠️ 이 문서는 작성 시점의 스냅샷이다 (2026-07-31 확인)
>
> 갭 분석은 «그때 무엇이 비어 있었나»를 남기는 게 목적이라 상태표를 계속 갱신하지 않는다.
> 다만 **2절(전투·보스)은 그 뒤로 절반이 채워져서** 그대로 두면 오독된다 — 아래 2절 표만 갱신했다.
> 나머지 절의 상태는 `docs/TODO.md` 와 `docs/SELF-CHECK-2026-07-31.md` 가 최신이다.
>
> **줄 번호 참조 주의.** 이 문서의 `파일.js:123` 표기는 대부분 낡았다.
> `ls_siege.js` 하나만 해도 923 → 1082 줄로 자랐다. 검사법:
> ```bash
> python tools/check_line_refs.py
> ```
> 새로 쓸 때는 줄 번호 대신 **함수·상수 이름**으로 가리킨다.

---

## 0. 한눈에 보기

우리 서버는 **조사 보고서가 말한 "구조적 토대"를 이미 대부분 갖췄다.** 위협도 루프(0~15),
관문 진행 척추(T1~T4), 이중 성장(각성 5성 + 마을 4트랙), 정수 경제, 반복 루프(열쇠 던전·현상금·공성),
폐막식·통계·칭호까지 코드로 존재한다. STORY의 스토리 요소도 대부분 시스템에 대응된다
(STORY 부록B의 매핑 표가 이를 확인해 줌).

**따라서 남은 작업은 "새 시스템 발명"이 아니라 대체로 세 종류다:**
1. **질감(연출) 채우기** — 이미 도는 시스템에 예고·물결·성좌 중계 같은 "손맛"을 입히기 (RESEARCH의 반복 결론 ①③④).
2. **밤/월드에 "밖에 나갈 이유" 붙이기** — 지금 밤은 방어 일변도, 월드는 정적이다 (RESEARCH 3·4의 최대 갭).
3. **안내자(호데고스)의 목소리를 코드에 심기** — STORY의 화자가 아직 인게임에 없다 (STORY의 최대 갭).

| RESEARCH 영역 | ✅ | 🟡 | 🔲 | 갭 요약 |
|---|---|---|---|---|
| 1. 성장 | 2 | 2 | 2 | 각성 질적 계단(2·4성)·5성 이후 수평 성장 없음 |
| 2. 전투·보스 | **3** | 0 | **3** | ~~전무~~ → 어휘 5색·부활 규칙·소프트 격노 완성. 그로기·체크포인트·공성 미니보스가 남음 |
| 3. 공성·밤 | 1 | 2 | 6 | **밤 전용 보상 없음**(방어 일변도)·능동 반격·희망 게이지 없음 |
| 4. 월드·정화 | 0 | 3 | 3 | 정화 연출 약함·오염 확산/최종문 실루엣 없음 |
| 5. 스토리·연출 | 2 | 3 | 2 | **호데고스 인게임 대사·수기 12편·오프닝 시퀀스 없음** |
| 6. 운영·수명 | 0 | 3 | 4 | 주간 금고·챕터 캘린더·FTB 퀘스트북 없음 (Phase 7 예정) |
| 7. 반면교사 | ✅ 대부분 회피 성공 | | | 확률강화·PvP·하드격노 없음. 스펀지 회피는 `ls_config` 주석에 명문화 |

---

## 1. 성장 시스템 (각성·스킬트리·유물)

현: `ls_ascend.js`(1~5성, 피해 ×1.5~3.0 + 직업별 체력), `lsrelics` 모드(무기별 스킬), `puffish_skills`(스킬트리).

| 후보 (RESEARCH 1) | 상태 | 현 구현 | 추가/수정 제안 |
|---|---|---|---|
| 각성마다 질적 변화 1개 | 🟡 | 3성 추가스킬·5성 궁극기는 `lsrelics`가 해금(`ls_ascend.js:143-144`). 2·4성은 **순수 수치**(피해·체력)뿐 | 2성=이동기 강화, 4성=패시브 2단계처럼 **모든 각성에 질적 변화**를 넣어 "3성 찍으면 달라진다"를 4단계로. lsrelics 스킬 게이팅 확장 |
| 각성 순간 최상급 연출 | ✅ | 타이틀+사운드3종+파티클2종+전체 공지+즉시회복(`ls_ascend.js:130-142`) | 거의 완성. **주변 몹 밀치는 충격파 1회**(`particle`+`damage`/knockback)만 얹으면 조사 후보 충족 |
| 5성 이후 유물 외형 진화 | 🔲 | 5성이 캡, 이후 성장축 없음 | **누적 업적(공성 방어·정화·보스 킬 수)으로 무기 발광 단계 상승**(전투 스탯 0). 3~6인은 캡 도달이 빨라 이게 없으면 5성 직후 고갈 (RESEARCH 0-1) |
| 8직업 핵심 기둥 문장 | 🟡 | `FATES[].story` 3줄 로어는 있음(`ls_fate.js:36-115`). "리워크 판단 기준" 문장은 미정의 | 각 유물 1문장 확정(예: "게볼그=간격 지배", "스틱스=이동기가 곧 딜사이클") — 밸런싱 헌장. **문서 작업**(lsrelics 리워크 가드레일) |
| 전멸 천장(pity) | 🔲 | 없음 | 관문 보스 N회 전멸 시 다음 도전 피해 +5% 누적 → "언젠가 반드시 깬다" 보장. `ls_rift`에 카운터 추가 |
| 드롭 연출 3층 표준화 | 🔲 | Apotheosis 기본 드롭음만 | 일반/상급/유물급 사운드·파티클 표준. 다만 이건 대체로 모드(Apotheosis) 설정으로 처리 가능 — 우선순위 낮음 |

## 2. 전투·보스전 (관문 보스·기믹)

현: 관문 보스가 **전부 기성 모드 보스**(Cataclysm·BMD·Mowzie's). 커스텀 기믹 레이어 없음.

| 후보 (RESEARCH 2) | 상태 | 현 구현 | 추가/수정 제안 |
|---|---|---|---|
| 기믹 어휘 사전(색=의미) | ✅ | **`Telegraph.java` — 5색.** 빨강 피해라 / 노랑 뭉쳐라 / 보라 흩어져라 / 초록 지금 쳐라 / 파랑 멈춰라. 예고 1.5초 전 관문 공통 | 서버 공용 텔레그래프(빨강=피해라/노랑=뭉쳐라/보라=산개, 예고 1.5초 고정)를 KubeJS 파티클로. **난이도 높음** — 모드 보스에 얹기 까다로움 |
| 보스 그로기 게이지 | 🔲 | 없음 | 엔더링식 스탠스. 특정 스킬로 게이지↓, 만땅=그로기+피해 ×1.5. lsrelics 스킬 태그 연동 필요 — **난이도 높음** |
| 부활 규칙(무적+쇠약) | ✅ | **`ReviveRules.java` + `ls_revive.js`.** 5초 무적(공격 시 해제) + 별빛 쇠약 60초(주는 피해 −25%). 속성 모디파이어로는 원거리·스킬이 새어나가 `LivingIncomingDamageEvent` 로 옮겼다 | 부활 직후 5초 무적 + '별빛 쇠약' 60초(피해 -25%). `PlayerEvents.respawned`에 얹기 — **난이도 낮음, 효과 큼** |
| T4 체크포인트 | 🔲 | 최종 보스는 진(眞)형태 2페이즈지만(`ls_siege.js:150`) 체크포인트 아님 | 관문 보스 1페이즈 클리어 시 그 세션 2페이즈부터 재도전. 40분 보스 통째 재시작 방지 |
| 소프트 격노 | ✅ | **`ls_enrage.js`.** 교전 8분 후 30초마다 +10%, 상한 +200%. `/enrage status` | 전투 8분 경과 시 보스 공격력 +10%/30초 누적. `ls_rift` 보스 태그에 타이머 |
| 공성 마지막 웨이브 = 미니보스+기믹 | 🔲 | 공성 웨이브는 잡몹만(`buildWave` `ls_siege.js:255`) | 마지막 웨이브에 미니보스 1기 + 기믹 1개 → 공성이 관문 기믹 연습장이 됨 (FF14식 학습 구조) |

> **판단 (원문):** 이 영역은 통째로 "모드 보스 위에 커스텀 레이어 얹기"라 난이도가 가장 높다.
> **부활 규칙(낮음)** 하나만 먼저 취하고 나머지는 후순위 권장. 기믹 어휘/그로기는 lsrelics를 건드려야 해 별도 스코프.
>
> **2026-07-31 결과:** 어휘는 «난이도 높음» 판정이었지만 실제로는 - 모드 보스에 얹는 게 아니라 옆에 세우는 것 - 이라
> 풀렸다. 우리가 예고를 그리고 판정도 우리가 하며, 모드 보스의 공격에는 손대지 않는다.
> 그중 절반은 오히려 **모드가 이미 가진 규칙을 보이게 만든 것**이다 — 강철거인은 도끼가 땅에 박힌 2.9초·등 뒤 140°
> 에서만 맞는데 그걸 알리는 신호가 게임 안에 하나도 없었고, 이그니스는 반격 구간에 때리면 피해 0 + 반격이었다.
> 남은 셋 중 **그로기**는 여전히 모드 보스 AI 를 못 건드려 막혀 있고, **T4 체크포인트**는 최종 보스에만 현실적이다.

## 3. 공성·위협도·밤 (우리 시그니처 — 갭이 가장 큰 영역)

현: `ls_siege.js`(918줄, 위협도·성벽·웨이브·대공세·최종장), `ls_daynight.js`(커스텀 낮밤), `ls_beacon.js`(정화 봉화).

| 후보 (RESEARCH 3) | 상태 | 현 구현 | 추가/수정 제안 |
|---|---|---|---|
| 공성 예고 하루 단계화 | 🟡 | 대공세 D-3/D-1/당일 채팅 예고(`ls_siege.js:530-542`) + 상단 날짜 보스바 + 혈월(대공세) | **당일 아침부터 액션바 붉게 → 저녁 천둥·하늘 → 밤 뿔피리** 단계 시각 연출 추가(7DTD 블러드문). `ls_daynight` 구간 훅 활용 |
| **밤 전용 보상 3종** | 🔲 | 밤 = 공성 방어 일변도 | ①밤에만 빛나는 채집물(RV 컬러조명 시너지) ②밤에만 열리는 균열 틈 미니던전 ③밤 사냥 시 다음날 위협 -1. **RESEARCH가 꼽은 밤의 최대 결함**(Dying Light: 밖에 나갈 이유 필요) |
| 균열의 능동 반격(전진 캠프) | 🔲 | 노드는 정적 장부(`riftnode`) | 위협 12+ 시 성역 방향에 전진 캠프(미니 스포너) 설치 → 낮에 안 부수면 밤 웨이브 +1. "낮=능동 정리, 밤=수동 방어" 리듬 완성 |
| 오염 영토 규칙 | 🔲 | 없음 | 노드 반경 96블록 = 봉화 설치·확장 불가 → 정화가 확장의 전제. `ls_beacon` 등록 시 노드 거리 체크 |
| 희망 게이지 | 🔲 | 봉화가 위협 하한만 낮춤(`ls_siege.js:58-66`) | 위협도의 짝. 봉화·관문·마을이 올리고 공성 패배가 내림. 단계별 전역 버프 + 상시 사이드바. Frostpunk 이중 게이지 |
| 광량-난이도-드롭 공식 | 🔲 | 없음 | 광원 없는 어둠에서 엘리트 확률·드롭 품질↑ → 셰이더 무드(RV)가 곧 게임플레이. `EntityEvents.spawned`에서 광량 체크 |
| 공성 몹 풀 = 관문 티어 교체 | 🟡 | `buildWave`가 **위협도(threatTier)로만** 몹 교체(`ls_siege.js:255-272`) | RESEARCH 2의 "나머지 절반": 관문 진행(`rf_progress`)에 따라 몹 **종류**를 새 팩션으로 교체(위협=양, 관문=질). buildWave에 tier 인자 추가 |
| 돌발 이벤트 "유성 낙하" | 🔲 | 없음 | 낮 무작위, 낙하 지점 어그로 + 희귀 광물. 정기(밤 공성) 밖의 비정기 이벤트가 루틴화 방지(PZ 헬리콥터) |
| 광량 0 배회 보스 | 🔲 | 없음 | 밤+완전 어둠에만 낮은 확률. "소문이 콘텐츠"(Darkest Dungeon 섐블러). 난이도 높음, 후순위 |

> **판단:** 시그니처 루프의 뼈대는 완성돼 있으나 **"밤에 밖으로 나갈 이유"가 0**이다. 밤 전용 보상 ③(밤 사냥→위협 -1)
> 하나만으로도 밤의 성격이 "농성"에서 "선택"으로 바뀐다. 능동 반격·희망 게이지는 기존 위협도/봉화 시스템에
> 자연스럽게 붙어 ROI가 높다.

## 4. 월드·정화 (수복의 손맛)

현: `ls_beacon.js`(정화 봉화), `ls_rift.js`(관문 클리어=월드티어), `ls_mobscale.js`(진행도 연동 몹 강화).

| 후보 (RESEARCH 4) | 상태 | 현 구현 | 추가/수정 제안 |
|---|---|---|---|
| 관문 파괴 = 세계 전환 | 🟡 | `completeTier`: 월드티어↑+금고+타이틀+피날레 무장(`ls_rift.js:177-206`) | **하늘에 별 하나 켜짐**(시각 연출)·밤 최소광량 소폭↑·새 엘리트 티어 해금 추가 → 보상과 위협 동시 상승. STORY 부록A "별이 돌아왔다" 연출을 코드로 |
| 균열 침식(오염 확산) | 🔲 | 없음 | 방치 노드 주변 검은 균사 주 단위 확산 ↔ 봉화 반경은 발광 식물 회복 → "지도가 병들고 낫는 게 보임". `fill`/`setblock` 스케줄러 |
| 봉화 점화 연출 최우선 | 🟡 | 타이틀+사운드+end_rod 파티클(`ls_beacon.js:44-47`) | **반경이 수 초에 걸쳐 물결처럼 밝아지는** 블록 치환 연출(반경별 딜레이). RV 환경에서 이게 서버 최대 차별화 포인트(RESEARCH 4 ★★) |
| 공용 시각 기호 3종 | 🔲 | 없음 | 8각 별/꺼진 등불/검은 덩굴 — 허브·제단·관문 반복 배치로 로어를 문법화. **건축·리소스팩 작업**(리셋 후) |
| 최종문 실루엣 | 🔲 | 없음 | 지형 위 거대 어둠의 첨탑 — 1챕터부터 보이는 목적지(엘든링 황금나무). **건축/스트럭처** |
| 전초 봉화 | 🔲 | 봉화는 정화 전용(리스폰 앵커 아님) | 원거리 공략용 소형 봉화(리스폰 앵커+미니 안전권) → 원정이 "왕복"이 아니라 "전선 밀어올리기"로. `ls_beacon` 변종 |

## 5. 스토리·연출 (STORY와의 갭 — 안내자의 목소리가 없다)

현: STORY.md에 세계관·문체 규칙·떡밥 대장부·연출 발췌가 **텍스트로** 완비. 그러나 **인게임 코드에 화자가 없음**.

| 후보 (RESEARCH 5 + STORY) | 상태 | 현 구현 | 추가/수정 제안 |
|---|---|---|---|
| 채널-목소리 규칙 | 🟡 | STORY 문체 규칙에 명문화(시스템/호데고스/프롤로그 3목소리) | **코드 표준화 부재** — 지금은 각 파일이 개별 `say()`/`title`. 3채널 연출 함수 라이브러리(`ls_voice.js`)로 통일 권장 |
| 안내자(호데고스) 대사 테이블 | 🔲 | STORY 부록A에 전멸 대사 5종 등 텍스트만 | **인게임 미구현 — 최대 갭.** {조건,우선순위,1회성,쿨다운} 구조 대사 테이블 + 트리거(전멸/첫T2/재접속 요약). Hades식 "실패가 대사를 부른다". 세션당 3~5회 예산(Bastion) |
| 별지기의 수기 12편 | 🔲 | STORY가 명시적으로 "미구현 — TODO(3번 문서)"로 지목 | 감정 아크(T1 희망→T4 유언) 배치. FTB 숨김 챕터 "기록 보관소" + 물리 기록의 방. 번호(3/12)로 수집욕 |
| 성좌식 중계 | 🟡 | 각성 시 전체 공지는 있음(`ls_ascend.js:141`) | 문구를 **성좌 문법으로 교체**: `[ 꺼진 별 하나가 'OO'을 바라봅니다 ]` → 각성 단계 따라 "바라봅니다→주목합니다→당신을 통해 빛나려 합니다" 동사 승급 |
| 플롯 훅 목록 운영 | ✅ | STORY 부록B 떡밥 대장부(위치·조건·회수 계획·거짓정보 NPC 후보) | 운영 문서로 완비. 실플레이 중 갱신만 |
| 유물 8종 전승 텍스트 | 🟡 | `RELICS[].lore` 1줄씩(`ls_relic.js:11-31`) | "이전 시대 소유자(옛 별지기)" 전승으로 확장 → 8무기가 곧 세계관 문서 + 떡밥3(타락한 별지기) 회수 |

> **오프닝 "별이 꺼지는 밤"** (RESEARCH 5·6 공통): 🔲 첫 합동 세션 대본 미구현. 첫 공성 로직(`isFirstSiege`)은
> 있으나 "평화로운 저녁→하늘 어두워짐→타이틀→강제 공성→제단 발견" **연출 시퀀스**가 없다. STORY 부록A 텍스트를 KubeJS 시퀀스로.

## 6. 운영·수명 (3~6인 서버 생존 공식)

현: 폐막식(`ls_stats.js`), 칭호(`ls_title.js`), 백업(`server/backup.bat`), OP 커맨드 다수. FTB Quests는 미작성(CUSTOM.md Phase 7 예정).

| 후보 (RESEARCH 6) | 상태 | 현 구현 | 추가/수정 제안 |
|---|---|---|---|
| 주간 코어 세션 선언 | 🔲 | 없음(운영 정책) | "토요일 저녁 = 스토리의 날" 선언. 관문·챕터보스·대공세를 이 세션에 잠금 → 라이트 유저가 스토리 놓치는 게 구조적으로 불가 (Life 시리즈, RESEARCH 6 ★★★) |
| 챕터 릴리스 캘린더 | 🔲 | FTB Quests 미작성 | 1막(재건)→2막(원정)→3막(근원) 2~3주 간격. KubeJS 게이트면 여는 비용=플래그 하나 |
| 주간 별빛 금고(Great Vault) | 🔲 | 없음 | 주간 목표 3칸(공성 3회·관문 재클리어 2회·현상금 5건)→주말 1개 선택 보상. **재클리어 이유를 만드는 검증된 장치.** `ls_bounty` 확장으로 구현 |
| 관문 입장권 어픽스 | 🟡 | 열쇠 던전 3종(`ls_keys.js`) 있으나 **랜덤 수식어 없음** | 티어+랜덤 어픽스 2~3종(WoW M+). 위협도를 자발적으로 올려 입장하면 보상 배율↑. `ls_keys` 확장 |
| 복구 커맨드 전수 감사 | 🟡 | 상당수 있음(`/fate reset`, `/ascend set`, `/expedition reset·setprogress`, `/mobscale set`, `/bossdiff`) | **`/town`에 레벨 set/reset 없음**·`ls_rescue`/`ls_beacon` 복구 커맨드 점검. DawnCraft 교훈: 모든 커스텀 플래그에 set/reset 필수 |
| 자동 백업 체계 | 🟡 | `server/backup.bat` 존재 | 시간당 스냅샷+일일 보관 **스케줄(작업 스케줄러)** 확인·문서화. 이벤트 전 수동 백업 습관 |
| 첫 30분 온보딩 시퀀스 | 🔲 | 가호 선택(`/fate`)·유물 흐름은 있음 | 접속→브리핑(1분,스킵)→가호→5분 첫 목표→마을. 오프닝 이벤트와 결합. 규칙은 위키 아닌 인게임(FTB 첫 챕터)에서 |

## 7. 반면교사 — 이미 잘 회피 중 (유지)

RESEARCH 7의 "하지 말 것"은 **대부분 이미 설계로 회피**돼 있다. 새로 추가할 것보다 **지키고 있음을 확인**하는 항목:

| 금지 (RESEARCH 7) | 현 상태 |
|---|---|
| 확률 재련/강화 | ✅ 없음 (각성=결정론적 정수 지불) |
| 숫자만 커지는 반복 | ✅ `ls_config.js` 주석이 "체력 스펀지 회피, 긴장은 공격력에서" 명문화 |
| PvP 약탈·자원 경쟁 | ✅ `pvp=false`, 공용 창고·공동 금고 설계 |
| 하드 격노(시간초과 즉사) | ✅ 없음 (최종 보스는 체력↔밤진행 매핑, 소프트) |
| 읽을 수 없는 죽음 | 🟡 부활 규칙·전투 리포트 추가하면 완전 회피 (→ 2·6 참고) |
| 시즌 캐릭터 리셋 강제 | ✅ 폐막식 후 "리셋 여부는 투표"(`ls_stats.js` 설계) |
| 전 시스템 첫날 공개 | 🟡 챕터 게이팅(6번) 도입 시 완성 |

---

## 8. 스토리 ↔ 시스템 정합성 (교차 확인)

STORY의 세계관 명사가 코드와 **정확히 일치**한다 — 재작업 없이 연결 가능:

- 8가호 이름(아틀라스…쿠훌린) = `ls_fate.js FATES` ✅
- 8유물 이름(에테르 이지스…게볼그) = `ls_relic.js RELICS` ✅
- 각성 5걸음 = `ls_ascend.js` 1~5성 ✅
- 네 관문(개척/심층/정점/균열핵) = `ls_rift.js TIERS` ✅
- 위협도 0~15 = `ls_siege.js MAX_THREAT` ✅
- 정화 봉화 = `ls_beacon.js` ✅ · 균열 정수 = `kubejs:rift_essence` ✅
- "세계가 밝아지면 그림자도 깊어진다"(6장) = `ls_mobscale.js`의 서사적 정당화 ✅

> **결론:** STORY는 시스템의 사후 서사가 아니라 **설계와 맞물려 쓰였다.** 코드 이름을 바꿀 필요가 없고,
> 남은 일은 "그 서사를 플레이어가 인게임에서 듣게" 만드는 것 — 즉 §5의 호데고스 대사·수기·오프닝이 핵심 갭이다.
