# Last Stardust — 서버 오픈 준비

> 이 문서는 **「열기 전에 무엇을 해야 하나」** 하나만 다룬다.
> 콘텐츠 설계는 `TODO.md`, 검증 절차는 `TEST-PLAN.md`, 수치 근거는 `DECISIONS.md` 다.

---

## A. 열기 전에 반드시 (블로커 5)

### A-1. 🔴 화이트리스트가 꺼져 있다

`server/server.properties` 가 지금 이렇다:

```
white-list=false
enforce-whitelist=false
online-mode=true
max-players=8
```

**주소만 알면 아무나 들어온다.** 8인 지인 서버에 이 상태로 열면 안 된다.

```
white-list=true
enforce-whitelist=true
```
로 바꾸고 **`/whitelist add <닉>` 을 인원수만큼** 미리 넣어 둔다.
`enforce-whitelist=true` 까지 켜야 **이미 접속 중인 비허용자도 끊긴다.**

### A-2. 🔴 ~~월드 리셋~~ → **지금 월드로 간다** (2026-08-12, 유저 결정)

리셋은 2026-08-06 부터 예정돼 있었지만 **접었다.** 스폰 근처에 자연 생성된 **비행선**이 있고,
그걸 온보딩의 시작점으로 쓰기로 했다(§B-3) — 리셋하면 그게 사라진다.

**대신 두 가지가 따라온다:**

**① 리셋이 해 주던 청소를 손으로 해야 한다.** 지금 월드는 시험판이다 —
오늘만 보스를 여덟 번 소환하고 격노를 걸었다 풀었고 `/dummy` 더미가 곳곳에 서 있다.

```
/dummy clear                  훈련 더미
/lsgimmick clear              태그 붙은 시험 보스
/kill @e[type=...]            ⚠️ 바닐라 /summon 으로 부른 보스는 위로 안 지워진다
/enrage clear                 격노 잔재 (⚡ 표식으로 찾는다)
/bossdiff reset               시험용 오버라이드
/rkey clear · /vault reset    서약·금고 시험 진행도
```

계정도 시험 상태다 — `a_ka1128` 이 5성 유물을 들고 있고 가호·각성·금고가 다 차 있다.
`/fate reset` · `/relic revoke` · `/ascend set 1` 로 되돌린다.
**금고·마을 레벨·관문 진행도**도 시험값이 들어 있는지 `/town` · `/expedition` 으로 확인할 것.

**② 리셋을 기다리던 항목들의 처지가 바뀐다:**

| | 리셋 전제였을 때 | 지금 |
|---|---|---|
| **제단 자연생성** | 월드젠으로 심는다 | ❌ **불가능** — 구조물은 이미 생성된 청크엔 안 나온다. **손으로 짓는 수밖에 없다** |
| `ls_migrate.js` 삭제 | 리셋 후 한 판 돌려보고 | ⏸ 근거가 「리셋이 예정됐다」였다. **다시 판단해야 한다** |
| 균열 노드 배치 | 리셋 후 | ✅ **지금 해도 된다** |

### A-3. 🟡 성역 좌표 지정

새 월드에는 성역이 없다. **없으면 공성·관문·봉화가 통째로 안 돈다.**

```
/sanctuary set
```

첫 세션 시작 지점을 정한 뒤 곧바로 찍는다.

### A-4. 🟡 백업이 실제로 도는지

`server/backup.bat` 는 **있다.** 다만 **자동 실행이 걸려 있는지는 확인 안 됐다** —
작업 스케줄러에 등록돼 있는지, 어디에 몇 개까지 쌓는지.

리셋 직후 **첫 백업이 실제로 파일로 떨어지는 것**을 눈으로 한 번 봐야 한다.
「돌고 있다고 생각했는데 아니었다」가 제일 비싼 실패다.

### A-5. 🟡 접속 경로

포트포워딩(25565) · 공인 IP 또는 도메인 · 방화벽.
`difficulty=hard` 는 그대로 둔다 — **오늘의 모든 수치가 Hard 기준으로 측정됐다**
(`mobAttack` 이 난이도 배수 ×1.5 를 탄다 — `BLESSING.md`).

---

## B. 첫 세션 (온보딩 30분)

정해진 흐름은 이미 있다:

```
접속 → 프롤로그 자동 재생(약 30초) → /fate 선택 화면 → 시작 키트
     → 첫 공성 격퇴 → 별의 파편 1 → 제단에 바침 → 유물 획득(1성)
```

- **가호는 12종이고 한 사람이 하나씩 «독점»한다** — 남이 고른 가호는 못 고른다
  (`ls_fate.js` 의 `FATE_KEYS` 12개 · `ftTakenBy` 가 막는다). 정원이 8명이라 자리는 남지만,
  인기 가호가 겹치면 먼저 누른 쪽이 가져간다. 미리 정해 두는 편이 낫다
  — 「8종」이라고 적혀 있던 건 가호가 열둘로 늘기 전 숫자다
- 프롤로그 다시 보기: `/lsrelic prologue`
- 유물 카드 12종은 **디스코드에 이미 게시**돼 있다

**⚠️ 오프닝 「별이 꺼지는 밤」 대본은 아직 없다**(`TODO.md` D절).
없어도 서버는 돌지만, 첫 세션의 분위기를 잡는 물건이라 여기만 손으로 메꿔야 한다.

---

## B-2. 🔴 「보여주는 화면」은 있는데 **「보이는 세계」가 없다**

UI 쪽은 오히려 잘 갖춰져 있다 — 마을 화면(**M**) · `/bless` 창 · 사이드바 `희망`/`위협` ·
봉화 점화 물결 · 보스 예고 테두리 파티클 · 유물 툴팁과 전승 2행 · 디스코드 카드 12종.

**빈 건 월드 쪽이다.** 시스템이 전부 숫자와 명령으로만 존재한다.

### ① 성역이 «좌표 한 점»이다 — 제일 큰 빈칸

공성의 판돈이 **성벽 내구도 3000 + 방벽Lv×1000** 인데, **그 성벽이 물리적으로 없다.**
지금은 반경 24 의 보이지 않는 선이고, 몹은 거기서 밀려나며 «무언가»를 두드린다.

코드는 처음부터 **누군가 짓는다는 전제**로 쓰였다:

```js
const WALL_DEFAULT_R = 24     // 성벽 반경 기본값 (실제 성벽에 맞춰 /wall radius 로 조정)
// 신호기 등 중심물은 장식 — 체력은 성벽 자체에 있다. 방벽(ramparts) 레벨 = 성벽 내구도.
```

**리셋하면 맨땅에서 시작한다.** 성벽을 짓고 `/wall radius <n>` 으로 맞추지 않으면
「밤새 보이지 않는 벽을 지킨다」가 된다 — 공성이 이 서버의 중심 루프인데.

> 최소한: 반경 24 짜리 벽 하나. 모양은 `ls_config.js` 에서 원형/정사각을 고른다.
> 문·망루 같은 건 나중에 붙여도 되지만 **벽 자체는 첫 공성 전에 있어야 한다.**

#### ⚠️ 반경을 키울 땐 «시뮬레이션 거리»를 같이 본다 (2026-08-12)

공성 몹은 성벽 «닿는 끝」에서 8칸 밖에 스폰된다(`spawnRing = wallReach + 8`).
**그 거리가 시뮬레이션 거리를 넘으면 몹이 스폰만 되고 틱을 안 받아 그 자리에 서 있는다** —
오류도 로그도 없이 밤이 그냥 지나간다.

| 반경 | 정사각 (R×√2+8) | 원형 (R+8) |
|---:|---:|---:|
| 24 | 42칸 | 32칸 |
| 60 | 93칸 | 68칸 |
| **120** | **178칸** | 128칸 |

큰 성을 감싸려고 **반경 상한을 60 → 200 으로 열었고**(`ls_siege.js`),
`simulation-distance` 를 **8 → 12**(192칸)로 올렸다.

**그리고 계기를 붙였다.** `/wall` 과 `/wall radius` 가 스폰 거리 > 시뮬레이션 거리이면
빨갛게 경고하고 필요한 값까지 알려준다. 조용히 실패하는 종류라 반드시 보여야 한다 —
오늘 하루 「계기가 값을 쥐고도 안 보여줘서」 네 번 헤맸다(`CLASSES.md` 「계기 구멍」).

> 📌 **공성은 이미 단방향이다** — `ls_siege.js:463` 이 진격 방향 ±35° 부채꼴에서만 스폰한다.
> 사방에서 오지 않는다.

#### ✅ 여기까지 끝났다 (2026-08-12)

- **성벽을 지었다** — 정사각 반경 60 (한 변 120칸), 성역이 중앙.
- **진격 방향을 성문 쪽으로 고정했다** — `ls_config.js` 의 `wall.siegeAngle: 90`(남).
  `null` 로 두면 예전처럼 매 공성 무작위. 각도는 `x=cos · z=sin` → **0=동 · 90=남 · 180=서 · 270=북**.
- **인게임에서 확인했다** — 몹이 성문 앞에서만, 성벽 33칸 바깥에서 나온다.

> `simulation-distance` 는 12 로 올려 뒀는데 **반경 60(93칸)이면 8(128칸)로도 충분하다.**
> 되돌리면 서버가 가벼워진다 — 반경을 120 근처까지 다시 키울 게 아니라면.

### ② 마을 16단계가 월드에서 하나도 안 자란다

4트랙(방벽 ▦ · 공방 ⚙ · 성소 · 구역) × 4레벨 = **16단계**. 금고를 붓고 레벨을 올려도
**화면 속 숫자와 효과만 바뀌고 세계는 그대로다.**

이 서버의 서사가 「재건」인데 **재건이 안 보인다.** 레벨마다 블록 몇 개씩만 놓아도
(방벽 = 벽이 두꺼워짐 · 공방 = 모루/화로가 늘어남 · 성소 = 제단이 커짐 · 구역 = 집이 생김)
전혀 다른 물건이 된다.

> 구현은 어렵지 않다 — 레벨업 훅에서 `/fill`·구조물 붙여넣기 한 번이면 된다.
> **정해야 할 건 「무엇이 자라는가」이지 「어떻게 자라는가」가 아니다.**

### ③ 유물 제단이 lodestone 한 칸

**가호 12종 = 제단 12곳** (`ls_relic.js` 의 `RELICS` 키 수와 정확히 같다 — 예전에 「9~10곳」이라
적혀 있던 건 유물이 열둘로 늘기 전 숫자다). **손으로 짓고 `/relic altar <가호>` 로 등록**한다.

> ⚠️ **월드젠 경로는 막혔다.** 데이터팩 구조물은 «이미 생성된 청크»엔 안 나오므로
> 리셋을 접은 이상 쓸 수 없다(`TODO.md` C절). 손으로 짓는 게 유일한 길이다.

**건물은 하나만 만들어 12번 재사용하고 가호 색 블록만 바꾸는 게 낫다** —
일이 훨씬 적고, 「같은 양식의 제단」이 세계관상 더 맞는다.

#### 🟢 성벽 안에 모을 거면 **한 곳이면 된다** (2026-08-13)

가호별로 나누는 건 제단끼리 «멀 때» 값을 한다 — 「이 제단은 네 길이 아니다」가
찾아가는 수고를 전제로 한 문장이라서다. **성벽 안 한 자리에 열둘을 세우면**
그 문장은 뜻을 잃고 «내 것을 찾아 12번 우클릭하는» 일만 남는다.

```
lodestone 을 놓는다  →  그 위에 서서  /relic altar shared  →  우클릭해 확인
```

누가 우클릭하든 **그 사람의 가호에 맞는 유물**을 준다. 지급 규칙은 원래부터
가호를 보고 있었으므로(`rlGrant`), 바뀌는 것은 «어느 블록이 문을 여는가» 하나뿐이다.

> **보이는 맛은 건축으로 낸다** — 가운데에 «작동하는» 자석석 하나, 그 둘레에
> 가호 12색 기둥을 장식으로 세우면 그림은 그대로 남고 등록은 한 번으로 끝난다.
> 제단을 나눠 얻는 값은 «찾아가는 거리»지 «블록 개수»가 아니다.

`/relic altar` 는 공용이 등록돼 있으면 **「12개 중 몇 개」를 세지 않는다** —
안 그러면 「1/13」이 떠서 열둘을 더 지어야 하는 줄 알게 된다.

#### 가호별로 흩어 놓을 거면 (12곳)

절차는 제단 하나당 세 줄이다:

```
lodestone 을 놓는다  →  그 위에 서서  /relic altar <가호>  →  그 자리에서 우클릭해 확인
```

- 등록은 **lodestone 을 직접 찾아서** 한다(발밑 −1 · 선 자리 · −2 까지 훑는다).
  발밑에 없으면 등록을 **거부**한다 — 예전엔 발 높이를 그대로 저장해서 한 칸 어긋난 채
  «✔ 등록» 만 뜨고 우클릭이 영영 안 먹었다.
- 같은 블록에 두 가호를 등록하면 거부한다.
- **`/relic altar`** (인수 없이) 로 12개 중 몇 개가 등록됐는지·좌표가 어디인지 한 번에 본다.
  리터럴은 하나뿐이다 — **인수가 있으면 등록, 없으면 현황.** 잠깐 `altars` 를 따로 뒀다가
  한 글자 차이로 `/relic altars chiron` 이 튕겨서 합쳤다.

### ④ 공용 시각 기호 3종 · 최종문 실루엣

8각 별 / 꺼진 등불 / 검은 덩굴 — **어디에도 안 쓰이고 있다.** 이게 있어야
「여긴 별지기의 자리」와 「여긴 삼켜진 곳」이 말 없이 갈린다.
최종문(어둠의 첨탑)은 지평선에 보이는 목표물이라 **첫날부터 보여야 값을 한다.**

### ⑤ 유물 모델 둘이 «자리 맡기»다

헤카테·하르모니아는 **같은 낫을 재채색한 것**이다.
`models/item/*.json` + `textures/item/*.png` **두 파일만 덮으면 교체된다** — 코드는 안 바뀐다.

---

## B-3. 비행선 NPC 온보딩 (2026-08-12 구현, ⚠️ 미검증)

스폰 근처에 자연 생성된 **비행선**을 첫 세션의 시작점으로 쓴다.
**월드 리셋을 접은 이유가 이거다** — 리셋하면 그 배가 사라진다.

```
접속(비행선) → 프롤로그 자동 재생 → NPC 「난파선의 생존자」 클릭
    → 대화 (여기가 어디 / 당신은 누구 / 성역으로)
    → [ 성역으로 간다 ] → 텔레포트 + 연출 → 3초 뒤 가호 선택 화면
```

### 만든 것

| | |
|---|---|
| `Onboarding.java` | 보내고 · 떠나는 자리 연출 · 도착 연출 · 가호 화면. **절차 전부** |
| `/lsonboard <이름>` | NPC 가 부르는 입구. 관리자도 손으로 부를 수 있다 |
| `FateAutoOpen.openLater()` | 프롤로그 없이 가호 화면만 N틱 뒤에 |
| `ls_wreck_survivor.npc.snbt` | NPC 프리셋 — **데이터팩 파일이라 월드와 무관하게 산다** |

**절차를 코드에 둔 이유:** Easy NPC 대화 버튼은 커맨드를 한 줄씩만 부른다.
넷을 커맨드로 늘어놓으면 **절차가 데이터 파일 안에 흩어지고**, 순서 하나 바꾸려면
게임 안에서 NPC 를 편집해야 한다. **NPC 는 한 번만 부른다.**

**도착지는 성역 좌표 하나뿐이다**(`LSData`). 귀환석이 쓰는 것과 같아서,
첫 이동과 이후의 모든 귀환이 자동으로 같은 자리를 가리킨다.

### 세우는 법

프리셋은 **데이터팩 파일**이라 고친 뒤 `/reload` 를 해야 목록에 뜬다.

```
/reload
/easy_npc preset import default <탭>          ← 새로 하나 세운다
```

`import` 아래 갈래는 `default` · `world` · `custom` · `data` · `local` 다
(jar 의 `PresetImportCommand` 에서 확인). 우리 파일은
`kubejs/data/easy_npc/**default_preset**/humanoid/` 에 있으므로 **`default`** 다.
뒤의 이름·좌표·소유자는 **탭 완성으로** 고른다 — 인수 순서는 버전마다 바뀐다.

비행선 갑판에 세우고 — `NoAI` · `Invulnerable` · `Silent` 가 프리셋에 들어 있으니
안 움직이고 안 죽고 조용하다.

#### ⚠️ 프리셋 파일을 고칠 때 (2026-08-12에 겪음)

**고쳤으면 반드시 먼저 돌린다** — 아래 셋 다 인게임에서 이유를 안 알려준다:

```powershell
py tools/check_snbt.py server/kubejs/data/easy_npc/default_preset/humanoid/ls_wreck_survivor.npc.snbt
```

- 🔴 **`data` 바로 아래에 `id`(엔티티 타입)가 있어야 한다** — `id:"easy_npc:humanoid"`.
  **`PresetMetadata.entityTypeId` 는 별개다.** 그게 있어도 `data.id` 가 없으면 실패한다.
  인게임에는 §c「Unable to import default preset …」§r 한 줄만 뜨고, 진짜 이유는
  **서버 로그에만** 남는다 — `Missing entity ID tag in preset data`.
  **막히면 채팅 말고 `logs/latest.log` 를 본다.**
- **SNBT 에는 주석을 못 넣는다.** `//` 를 한 줄 넣으면 파서가 거기서 죽고,
  **프리셋이 목록에서 조용히 빠진다.** 설명은 이 문서에 적는다.
- **`ActionPermissionLevel` 은 `ActionEventSet` 의 «형제»다.** 안에 넣으면 무시되고
  기본 권한으로 떨어져 `/lsonboard`(권한 2)가 튕긴다 — 증상은 「대화는 되는데
  마지막 버튼만 아무 일도 안 일어난다」라 찾기 어렵다.
  실물은 `world/easy_npc/npcs/<uuid>.npc.nbt` 를 열어 보면 된다(gzip NBT).
- **`ON_INTERACTION` 에는 대화만 둔다.** 인게임에서 만든 NPC 의 기본값에는
  `OPEN_TRADING_SCREEN` 이 같이 들어 있어, 클릭하면 거래창이 함께 뜬다.
- **프리셋은 «통째로 갈아끼운다».** 모자란 칸만 채우는 게 아니다.
  그래서 `SkinData` 를 `Type:"DEFAULT"` 로 비워 두면 **가져오는 순간 스킨이 지워진다.**
  플레이어 스킨은 이름만으로는 안 붙고 `UUID`·`Timestamp` 까지 있어야 한다.

> 지금 파일은 인게임의 **린케우스**(`STEVE` + `HRSSamuelTTV` 플레이어 스킨)에 맞춰져 있다.
> 그래서 지금 세워 둔 NPC 에 그대로 가져와도 이름·스킨이 안 바뀐다.

### ⚠️ 세우기 전에 성역부터

`/sanctuary set` 이 안 돼 있으면 대화는 끝나는데 **아무 데도 안 간다.**
그 경우 `/lsonboard` 가 §c「성역이 아직 정해지지 않았다」§r 로 실패를 알린다 —
조용히 넘어가지 않게 해 뒀다. **첫 세션에 「대화는 끝났는데 아무 일도 안 일어났다」가 제일 나쁘다.**

### 같이 볼 것

- **스폰을 비행선으로 고정** — `/setworldspawn`. 안 하면 첫 접속이 엉뚱한 데서 시작한다
- **대사는 초안이다.** 세계관은 프롤로그(`FateAutoOpen.PROLOGUE`)와 맞춰 뒀지만
  말투·분량은 고쳐 쓸 자리다. 파일 하나만 고치면 된다
- 비행선을 **구조물(`.nbt`)로 떠 두면** 나중에 리셋하더라도 살릴 수 있다

---

## C. 열고 나서 확인할 것 — **여기가 진짜 목록이다**

혼자서는 더 못 잰다. `⚠️ 실전 미검증` 이 **16개** 남아 있고, 대부분 4인이 있어야 한다.

### C-1. 🔴 파나케이아 + 케이론 = 14 HPS

**남은 것 중 제일 큰 위험.** 힐러 둘이 겹치면 파티가 안 죽는다 —
그러면 오늘 맞춘 보스 수치가 통째로 무의미해진다.
둘 다 있는 판을 **한 번은 반드시** 겪어야 한다.

### C-2. 보스 난이도 — 셋이 물러진 채로 남아 있다

| 5성 원거리(80HP) 기준 | 예전 | 지금 |
|---|---:|---:|
| 강철거인 | 1.4대 | 2.2대 |
| 이그니스 | 2.7대 | 3.9대 |
| 건틀렛 | 3.0대 | **3.1대** ← 복원됨 |
| 괴물 | 2.1대 | 3.3대 |

`dmg%` 로는 더 못 올린다(`TEST-PLAN.md` 3-C). 남은 손잡이는 **보스 체력**뿐인데
그건 「4명 60초」 설계선을 건드린다. **4인 한 판을 겪고 정한다.**

전투 기록계가 판마다 「목표 60초 → 권장 체력 N」을 스스로 뽑아 주니 그 숫자를 쓰면 된다.

### C-3. 🔴 강철거인이 1성을 평타 한 대에 죽인다

T1 보스인데 평균 37.1 · 최대 67.5 다. 1성 체력이 32~40 HP 니 **근접·원거리는 원콤**이고
탱커도 1.08대다. `dmg 100` 으로도 그렇다.

느리고 예고가 큰 보스라 실전에선 대부분 피하겠지만, **첫 관문에서 원콤이 나면
배울 기회가 없다.** 4인이 실제로 어떻게 겪는지 보고 정한다.

### C-4. B절 5종 — 한 번도 실전에서 안 돌았다

공성 예고 단계화 · 봉화 점화 물결 · 희망 게이지 · 공성 몹 티어 교체 · 보스 기믹 5종.

### C-5. 5성 2단 — 8종 미검증

4종은 실측 완료(이지스·타이탄·펠리온·셀레스티아). 남은 여덟은
**처치**(스틱스·헤스페로스) · **패링 타이밍**(아드라스테이아) ·
**아군 필요**(파나케이아·바르비톤) · **눈으로 보면 끝**(게볼그·솔라리스·시리우스).

### C-6. 주간 금고 · 균열 서약

명령과 훅은 전부 확인됐다. 남은 건 **여럿이 도는 판에서**
공성 적립이 관전자 빼고 전원에게 가는지, 서약 몹 강화가 파티 전체에 걸리는지.

---

## D. 열고 나서 천천히 (막지 않는 것)

균열 능동 반격 · 밤 전용 보상 · 균열 침식 · 유성 낙하 · 전초 봉화 ·
5성 이후 외형 진화 · 수기 12편 · 건축 문법 · 최종문 실루엣 · 경제 싱크.

전부 `TODO.md` 에 있고 **하나도 오픈을 막지 않는다.**

---

## 요약 — 순서대로

```
1. 화이트리스트 켜고 인원 등록          ← 이게 제일 급하다
2. 시험 흔적 청소 (A-2 의 명령 목록)     ← 리셋을 안 하니 손으로
3. /sanctuary set
4. ✅ 성벽 — 지었다 (반경 60 · 진격 남쪽 고정 · 인게임 확인 완료)
5. 유물 제단 12곳 — 손으로 짓는다        ← 월드젠 경로가 막혔다 (/relic altar 로 확인)
6. 비행선 NPC 온보딩 (§B-3)
7. 백업이 실제로 도는지 눈으로 확인
8. 열고, C절을 겪으면서 채운다
```

**4·5 는 코드가 아니라 손으로 짓는 일이다.** 오늘까지의 작업이 전부 「숫자가 맞는가」였는데,
**남은 큰 덩어리는 「보이는가」 쪽이다.**

**C절은 열기 전에 못 채운다.** 그게 이 서버의 남은 미지수 전부이고,
혼자 재는 것으로는 여기까지가 끝이다.
