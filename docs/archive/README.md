# docs/archive — 시점 기록

여기 있는 문서는 **그때 무엇을 봤나**의 기록이다. 최신 사실로 덮어쓰지 않는다.
「지금 어떤가」를 알고 싶으면 `docs/` 의 살아 있는 문서를 본다.

| 문서 | 무엇 | 왜 안 지우나 |
|---|---|---|
| `CUSTOM.md` | 2026-07 Phase 7 착수 «전»에 그린 원본 설계도 | **왜 이 모양이 됐는지**가 여기 남아 있다. 이중 지갑·공동체 단위 성장 같은 판단이 지금 코드에도 살아 있다. 머리말의 「초안 → 실제」 대조표가 그 다리다 |
| `REVIEW.md` | 2026-07-31 갭 분석 (RESEARCH + STORY 대비 구현 대조) | 「그때 무엇이 비어 있었나」의 스냅샷. 지금 상태는 `TODO.md` 가 최신이다 |
| `SELF-CHECK-2026-07-31.md` | 2026-07-31 자가진단 | 진단서는 기록이라 갱신하지 않는다. `DECISIONS.md`·`TEST-PLAN.md` 가 이걸 근거로 인용한다 |

## 읽을 때 주의

- **줄 번호 참조가 낡았다.** `파일.js:123` 표기는 대부분 안 맞는다(`ls_siege.js` 하나만 해도
  923 → 1082 줄로 자랐다). 검사: `python tools/check_line_refs.py`
- 상태표의 ✅/🟡/🔲 는 **작성 시점** 값이다.
- 여기 내용을 근거로 코드를 고치기 전에 반드시 `docs/TODO.md` 와 대조할 것.
