# 결정 대기 — 고르기만 하면 되는 것들

> 작성 2026-07-31. **여기 있는 건 전부 «내가 못 정하는 것»이다.** 밸런스 방향·운영 정책처럼
> 정답이 코드 안에 없고 당신의 취향과 그룹 성향이 정하는 항목들.
>
> 여태 이런 것들이 `TODO.md` E절과 `SELF-CHECK` 5절에 «미결»이라고만 적혀 있었다.
> 그러면 볼 때마다 «그래서 뭘 고르면 어떻게 되는데»부터 다시 조사해야 한다.
> 그 조사를 미리 해서 붙였다 — **각 항목은 선택지와 그 결과 수치까지 계산돼 있다.**
>
> 고른 뒤에는 그 항목을 «결정됨»으로 옮기고 근거를 남긴다. 나중에 다시 열 때
> «왜 이걸 골랐더라»가 없으면 같은 논의를 처음부터 하게 된다.
>
> **지금 열려 있는 건 4번(별의 축복) 하나다.** 그것도 「부여 시점」과 「재감정」 둘뿐 —
> 범위는 12종·전부 Java 로 정해졌다(4-3).
> 1·2 는 2026-07-31 에, 3·5 는 07-25 에, 6 은 07-31 에 닫혔다.
>
> 닫힌 절을 지우지 않고 두는 이유는 3번이 실제로 두 번이나 다시 열렸기 때문이다 —
> 닫힌 기록이 없으면 「미결」로 되돌아온다. 원래 분석은 `N-A` 절로 남겨둔다(틀렸던 부분 포함).

---

## 1. ~~스틱스 격차~~ — **닫힘 (2026-07-31 유저 결정): 기본치 ×1.15**

> **결정: `LSRelics.ASSASSIN` 아이템 공격력 1.225 → 1.409.** 축복(덧셈)은 안 건드린다.
>
> **목표가 「평균 맞추기」에서 「난이도에 대한 보상」으로 바뀌었다.** 유저가 잡은 조건은
> *「더미에선 평균에 못 미치되, 백어택을 쓰면 평균을 넘는다」* 였다. 암살자는 등 뒤로 돌아야
> 숫자가 나오는 직업이고 그 수고가 보상받아야 한다는 것 — 편차를 0 으로 만드는 게 아니다.
>
> ### ✅ 실측 완료 (2026-08-04, 8종 전부 · 60초 · 축복 밖 · 방어도 0 · 5성)
>
> | 조건 | DPS | 파티 평균(88.7) 대비 |
> |---|---:|---:|
> | 정면 · 노크리 | **68.0** | **−23%** |
> | 풀딜(백어택+크리) | **97.0** | **+9%** |
>
> **목표를 양쪽 다 만족한다.** 더미에선 평균에 못 미치고, 제대로 쓰면 넘는다. 숙련 배수 ×1.43.
> 버프 없이 역산하면 정면 59(−33%) · 풀딜 84(−5%) — **잘 써도 평균에 못 미쳤다.** ×1.15 가 그 구멍을 메웠다.
>
> ⚠️ **정면 −23% 는 8종 중 가장 깊은 바닥이다.** 다른 일곱은 위치를 안 잡아도 그만큼 안 떨어진다.
> 의도한 설계지만, 스틱스를 고른 사람은 **조작이 익기 전까지 눈에 띄게 뒤처진다.**
> 남은 미측정: **백어택만(크리 없이)** — 「보통 플레이」의 기준선. 급하지 않다.
>
> ### ⚠️ 「50~70 대역」은 이제 잣대가 아니다
>
> 그 대역은 **2026-07-26** 측정이고, `GLOBAL_POWER ×1.8` 은 **07-30**(`4e5e71a`)에 들어갔다.
> 척도가 다르다. 내가 07-31 에 「대역 안에 있으니 안전하다」고 한 것은 **틀렸다** —
> 나흘 전에 무효가 된 가드레일을 근거로 삼았다.
> **지금 유일하게 쓸 수 있는 잣대는 «같은 날·같은 조건에서 잰 파티 평균»이다.**
> 절대 수치는 전역 배수가 바뀔 때마다 죽는다. 비교는 늘 같은 판에서 잰 것끼리.

### 시리우스 — 꼴찌가 아니라 **고장나 있었다** (2026-08-04 밤, 닫힘)

아래 표에서 꼴찌(78.9)였던 이유가 배율이 아니었다. **유성 사격의 폭발과 별빛 폭풍의 착탄
확산이 둘 다 안 돌고 있었다**(`TODO.md` D절 8). 훅을 살리고 재니:

| | 유성 사격 | 시리우스 지속 |
|---|---|---|
| 고장 상태 | 18타 **평균 9.9** | 78.9 (8종 꼴찌) |
| 훅 살린 뒤 | 18타 **평균 99.3 / 103.0** | **104.8** (압도적 1위) |

10배다. **폭발 상수 `12.71` 은 그때까지 한 번도 적용된 적이 없는 값이었다** — 가정하고
만들었는데 검증될 기회가 없었다. 유성 사격 혼자 전체의 31% 를 먹게 됐다.

> **결정: 폭발 12.71 → 9.53 (×0.75).** 지속 **97.9** — 솔라리스(97.0)·셀레스티아(99.1) 사이.
> 전체 평균(90.1)에 맞추는 5.72 안도 계산했지만, 그러면 시리우스가 다시 «원거리 셋 중 꼴찌»가
> 된다 — 방금 고친 바로 그 모양으로 되돌아간다. **원거리 셋이 상위권을 이루는 구도를 택했다.**
>
> ⚠️ 궁극기 몫(15.9)은 한 판씩의 추정이다. 이 무기는 궁극기가 ±20% 로 흔들려 「4판 이상 모아서
> 판단」이 원칙이다(`StarBow` 주석). 다시 잴 때 같이 볼 것.

## 1-B. 8종 총량 재조정 (2026-08-05 유저 결정 · 실행 완료)

08-04 실측표를 보고 **구도 자체**를 바꾸기로 했다. 문제는 시리우스 하나가 아니었다 —
**원거리 셋이 1·2·4위를 독식하는데, 원거리가 더 안전하다.** 붙어야 하는 이지스(80.5)·
게볼그(81.4)가 하위 두 자리인 게 그 구조를 그대로 보여줬다.

| 유물 | 전 | 배율 | 후 |
|---|---:|---:|---:|
| 시리우스 | 100.0 | — | 100 |
| 셀레스티아 | 99.1 | ×1.009 | 100 |
| 솔라리스 | 97.0 | ×1.031 | 100 |
| 타이탄 | 87.6 | ×1.096 | 96 |
| **게볼그** | 81.4 | **×1.179** | 96 |
| 이지스 | 80.5 | ×1.118 | 90 |
| 파나케이아 | 87.8 | — | **유지** |
| 스틱스 (정면 / 백어택 / 풀딜) | 68.0 / 81.6 / 97.0 | ×1.324 | **90 / 103.5 / 113.9** |

**파티 평균 88.7 → 96.7** (스틱스는 백어택 기준).

### 스틱스만 셋을 같이 만졌다

바닥을 90 으로 올리면 천장이 그대로 따라 뛴다 — **정면 90 이면 풀딜이 128.4 가 되어
2위 대비 +28%.** 형이 원한 천장은 104 였고, 둘은 동시에 성립하지 않는다.

그래서 배율 자체를 조였다. `BACKSTAB_BONUS` 1.20 → **1.17**, 크리 배수 1.5 → **1.26**
(신규 `onStyxCrit`). 결과 숙련 배수 **×1.43 → ×1.27**:

> 정면 90.0(7위) · 백어택만 103.5(1위) · 풀딜 113.9(2위 대비 +14%)

**크리를 깎고 백어택은 거의 그대로 둔 게 핵심이다.** 백어택을 깎으면 «등 뒤로 도는 이득»이
사라져 정체성이 죽고, 크리를 깎으면 «운 좋게 터지는 상단»만 눌린다. 조여야 하는 건 후자다
— `RelicEventHandlers` 주석이 원래 그렇게 적어뒀고, 이번에 그대로 따랐다.
상수가 1.15 가 아니라 1.17 인 건 백어택이 근접 직접 타격(89%)에만 붙기 때문이다.

### 구현: 유물별 배율 상수 하나 (`relicScale`)

여덟 군데를 각자 다른 방식으로 고치는 대신, **피해가 들어가는 순간 한 번 곱한다.**
평타가 나오는 자리가 유물마다 다르다 — 근접 넷은 아이템 공격력 속성, 시리우스는
`StarBow.ARROW_DMG`, 솔라리스는 `SolarMusket.RIFLE_DMG`, 셀레스티아는 아예 상수가 없다.
거기에 스킬 상수가 3~5개씩 더 붙는다. 한 곳으로 모으면 **다음 조정도 숫자 하나**다.

같은 이벤트에서 `onStyxStrike` 가 도는 게 실측으로 확인돼 있어(정면 68.0 vs 풀딜 97.0)
자리가 검증된 셈이다.

### 파나케이아를 유지한 이유

더미 87.8 은 이 유물의 실력이 아니다. 문서가 「더미로 재지 마라」고 못박아 뒀다.

| | 1성 | 5성 |
|---|---:|---:|
| 평타 회복 (2발/초) | 4 HPS | **10 HPS** |
| 자가 재생 | 1 HPS | 2.5 HPS |
| 심판의 빛 (쿨 8초) | 9.6 | **24** |
| 성역 (반경 4 · 8초) | 1.5/틱 | **3.75/틱** + 받는 피해 −20% |
| 소생 | 최대 체력 **50%** 로 부활 | |

힐은 일부러 완만하다 — 피해가 5.4배 오를 때 회복은 2.5배만 오른다
(*「회복까지 3배가 되면 파티가 잘 안 죽어 수성전의 긴장이 사라진다」*).
**딜 87.8 + 힐 10 HPS + 경감 20% + 소생이면 실전 기여는 딜 수치보다 확실히 위다.**

### 보스 체력도 같이 올렸다 (×1.09)

파티 평균이 8.0 (9%) 올랐으니 보스가 그만큼 빨리 죽는다. 안 올리면 「4명·60초」 목표가
55초짜리가 되고, 그건 그 값들이 존재하는 이유가 사라지는 것이다.

    강철거인 3650 → 3980 · 이그니스 7000 → 7630
    건틀렛  6250 → 6810 · 괴물    10400 → 11340

⚠️ **대략치다.** 정확한 값은 실전 한 판이 낸다 — 전투 기록계가 「목표 60초 → 권장 체력 N」과
붙여넣을 명령을 스스로 낸다.

> ⚠️ **전부 미검증이다.** 위 「후」 열은 실측이 아니라 배율을 곱한 예측값이다.
> 8종을 다시 재서 덮어쓸 것 — `TEST-PLAN.md` 5-3.

---

### 그날의 8종 실측 (2026-08-04) — 조정 **전** 기준선

| | 유물 | DPS | 평균 대비 | 평타 비중 |
|---|---|---:|---:|---:|
| 1 | 셀레스티아 | 99.1 | +12% | 41% |
| 2 | 스틱스(풀딜) | 97.0 | +9% | 60% |
| 2 | 솔라리스 | 97.0 | +9% | 35% |
| 4 | 파나케이아 | 87.8 | −1% | 81% |
| 5 | 타이탄 | 87.6 | −1% | 72% |
| 6 | 게볼그 | 81.4 | −8% | 56% |
| 7 | 이지스 | 80.5 | −9% | 66% |
| 8 | 시리우스 | 78.9 | −11% | 76% |

**편차 ±11%.** 모델(`calc_power_ratio.py`)은 ±26% 를 예측했다 — **절반 이하로 틀렸다.**
그 모델은 보정상수 k 가 ×1.8 이전 측정에 맞춰져 있어 지금 못 믿는다. 실측 8종이 생겼으니
그쪽으로 갈아타야 하는데, `GLOBAL_POWER` 가 유물 항에만 곱해지고 축복 덧셈엔 안 곱해지는
구조라 단순 교체가 안 된다. **재정비 전까지 이 표가 기준이다.**

### 왜 C(축복 +4.5)를 안 골랐나 — 이 문서가 틀렸던 부분

**이 절은 한때 「C안은 1성이 거의 그대로」라고 적고 있었다. 그게 틀렸다.**
축복은 **덧셈이고 성급을 안 탄다.** +1.5 를 더하면 5성에도 1성에도 똑같이 약 +6.2 DPS 가
붙는데, 1성 총량은 5성의 1/3 이라 비율로는 세 배가 된다:

| 축복 | 1성 | 5성 더미 |
|---|---|---|
| +3.0 (지금) | 35.5 (+5.5%) | 81.2 |
| +4.5 (C안) | **41.7 (+24.2%)** ← 게볼그 35.0 대비 압도적 1위 | 87.5 |

즉 **C안은 B안이 기각된 것과 똑같은 병을 앓는다** — 초반이 무너진다. 기본치는 곱셈이라
성급을 타므로 1성이 36.8 로만 움직인다. 그래서 기본치를 골랐다.

> **교훈:** 「곱셈이냐 덧셈이냐」를 안 보고 선택지를 비교하면 저성급이 조용히 망가진다.
> 이 표를 만들 때 5성만 계산했던 게 원인이다. **앞으로 밸런스 선택지에는 1성 열을 반드시 넣을 것.**

### 곁들여 — 곡선을 펴는 안도 있었다 (미채택)

진짜 진단은 「스틱스가 약하다」가 아니라 **「성급 곡선이 혼자 완만하다」**(1성 1위 → 5성 꼴찌)였다.
축복 +2.0 · 기본치 ×1.40 이면 1성 35.0 · 3성 61.5(−3.3%) · 5성 88.1(−5.9%) · 백어택 99.6(+6.4%)
로 곡선까지 펴진다. **두 곳을 고쳐야 하고 「스틱스만 축복을 덜 받는다」는 설정이 생겨서** 안 골랐다.
한 레버로 조건이 충족되면 그쪽이 낫다. 나중에 곡선 자체가 문제로 드러나면 이 안을 다시 열 것.

---

## 1-A. (원래 기록) 스틱스 격차 — 5성에서 혼자 뒤처진다

### 지금 상태 (`tools/calc_power_ratio.py` 실측)

| 유물 | 1성 | 5성 | 평균 대비 |
|---|---:|---:|---:|
| 스틱스 | **35.5** | **81.2** | **−13%** |
| 이지스 | 32.5 | 87.0 | −7% |
| 파나케이아 | 30.3 | 90.9 | −3% |
| 타이탄 | 33.6 | 92.2 | −2% |
| 게볼그 | 35.0 | 92.7 | −1% |
| 시리우스 | 33.6 | 100.8 | +8% |
| 솔라리스 | 34.0 | 102.1 | +9% |
| 셀레스티아 | 34.1 | 102.2 | **+9%** |

**1성에서는 스틱스가 2등인데 5성에서는 꼴찌다.** 편차가 ±12% → ±26% 로 벌어졌다.

### 왜 그런가

성역 축복의 Strength I 은 **덧셈 +3.0** 이고, 이건 `GLOBAL_POWER = 1.8` 을 안 탄다.
그래서 실효 배수가 이렇게 갈린다:

```
실효 배수 = (기본치 × 1.8 + 3.0) / (기본치 + 3.0)
```

기본치가 작을수록 분모에서 +3.0 의 몫이 커져 배수가 희석된다. 스틱스는 기본치가 제일 낮고
평타 비중이 제일 높아(= +3.0 을 가장 많이 받는 무기라) 가장 크게 손해를 본다.
1성에서 2등이던 것도 같은 이유다 — 그때는 덧셈이 이득이었다.

### 선택지

| | 무엇을 하나 | 결과 | 비용 |
|---|---|---|---|
| **A** | 그대로 둔다 | 편차 ±26% 유지 | 0 |
| **B** | 스틱스 기본치 ×1.15 | 5성 81 → 93 (평균) · **1성 35.5 → 39.5 로 압도적 1위** | 상단(백어택·크리) 대역 50~70 → 57~80 으로 같이 뜀 |
| **C** | 스틱스만 축복 덧셈 +3.0 → +4.5 | 5성 ≈ 85 (편차 ±18%) · 1성은 거의 그대로 | 「축복이 직업마다 다르다」는 설정이 생긴다 |
| **D** | `GLOBAL_POWER` 를 낮추고 보스 체력 전면 재역산 | 근본 원인(덧셈 희석)이 줄어든다 | 보스 4종 체력·유지율 전부 다시 |

**B 의 함정이 중요하다.** 5성을 맞추면 1성이 어긋난다. 두 끝을 동시에 맞추려면 덧셈 항을
건드리는 C 쪽이어야 한다.

> ⚠️ **B 를 고르면 50~70 대역이 같이 움직인다.** 그 대역은 당신이 직접 잡은 값이라
> 내가 임의로 못 건드린다. B 를 고를 거면 그 대역을 새로 잡을지도 같이 말해줘야 한다.

~~**추천: C.**~~ **철회.** 상단은 안 건드리지만 **저성급을 무너뜨린다**(위 표 참조).
이 추천을 쓸 때 5성만 계산하고 1성을 안 봤다.

### 그 50~70 대역이 무엇인가 (2026-07-26 실측 · 유저 결정)

> 이 표는 `docs/TEST-2026-07-26.md` 에 있던 것이다. 그 문서는 특정 jar(`946D0924`)의
> 일회성 검증 목록이라 2026-07-31 에 지웠는데, **여기 세 줄만은 결정의 근거**라 옮겨 왔다.

백어택 ×1.20 과 바닐라 크리 ×1.5 가 **근접 평타에만, 서로 곱해서** 붙는다.

| 숙련도 | DPS |
|---|---|
| 백어택 ✗ · 크리 ✗ | 45~51 |
| 백어택 ✓ · 크리 ✗ | **54.2 (실측)** |
| 백어택 ✓ · 크리 ✓ | 61~73 |

낙하 중에만 크리가 터져 상시 유지가 불가능하다. **그래서 이 상단을 허용하기로 했다(유저 결정).**
즉 위 표의 「임의로 못 건드린다」는 게으름이 아니라 이미 내려진 판단이다.

조이려면 `BACKSTAB_BONUS` 가 아니라 `CriticalHitEvent.setDamageMultiplier()` 를 써야 한다 —
이유는 `RelicEventHandlers.onStyxStrike` 위 주석에 있다.

### 곁들여 — 측정할 때 알아야 할 것 둘

- **솔라리스는 배율로 못 고친다.** 스코프 사격만 87, 플레어를 섞으면 30. 균일 배율은 이 격차를
  확대할 뿐이다. 스킬 자체의 피해/캐스팅 시간 구조를 봐야 한다. **미해결.**
- **파나케이아는 더미로 재지 마라.** 힐 우선 설계라 파티전에서는 평타가 거의 안 나간다(사선에
  아군이 늘 걸린다). 딜 기여를 성역 연소로 옮겼는데 이건 **적이 많을수록 비례해 커진다** —
  더미 1기에 7 DPS 면 몹 8기가 몰린 수성전에서는 56 DPS 몫이다. 힐량·피해 경감 20% 도 안 잡힌다.
  **수성전에서 판단할 것.**

---

## 2. ~~몹 스케일링의 「적대」 판별~~ — **닫힘 (2026-07-31 유저 결정): C안**

> **결정: `MobCategory == MONSTER` 로 좁힌다.** 구현: `LSKubeBridge.Api.isMonster()` +
> `ls_mobscale.js msIsHostile()`.
>
> 스크립트만으로는 못 하던 판정이다 — KubeJS 의 `e.getType()` 이 EntityType 이 아니라 **id
> 문자열**이라 `.getCategory()` 가 처음부터 예외만 던졌고, 그래서 판별이 계속 예비 경로
> (「공격력 속성이 있으면 적대」)로 떨어져 있었다. 자바에서는 한 줄이라 다리를 놓았다.
>
> **늑대·북극곰·벌이 빠지는 걸 알고 고른 값이다.** 「실제로 덤비는데 안 세지는」 경우가
> 생기지만, 규칙이 한 문장으로 설명되는 쪽(«몬스터만 세진다»)을 택했다.
> 추천은 B(철골렘·눈사람만 제외)였는데 유저가 C 를 골랐다 — 예외 목록을 관리하는 것보다
> 바닐라 분류에 맡기는 쪽이 오래 간다.
>
> ⚠️ **미검증.** 늑대에게 맞아 보고 스케일링이 안 붙는지 확인할 것. 예비 경로가 살아 있어서
> 다리가 실패하면 조용히 옛 동작(넓은 판정)으로 돌아간다 — 로그의 `ls_mobscale:isMonster` 경고를 볼 것.

---

## 2-A. (원래 기록) 몹 스케일링의 「적대」 판별 — 철골렘이 세진다

### 지금 상태

`ls_mobscale.js` 의 `msIsHostile()` 은 **공격력 속성이 있으면 적대**로 본다.

```js
return e.getAttribute('minecraft:generic.attack_damage') != null
```

원래는 `MobCategory == monster` 를 1순위로 보려 했는데, 그 코드는 `e.getType()` 이
id 문자열이라 처음부터 예외만 던지고 있었다(2026-07-25 에 죽은 경로로 확인·제거).

그래서 판별이 의도보다 **넓다.** 이런 것들이 관문 진행도에 따라 같이 세진다:

| | 세지는 게 맞나 |
|---|---|
| 철골렘 · 눈사람 | ❌ **플레이어가 만든 것이다.** 내 방어 수단이 강해지는 건 상관없지만, 마을 주민이 만든 골렘도 세진다 |
| 늑대 · 북극곰 · 벌 | 🤔 **실제로 덤빈다.** 세지는 게 오히려 맞을 수도 |
| 좀비 · 스켈레톤 등 | ✅ 의도대로 |

### 선택지

| | 무엇을 하나 | 비용 |
|---|---|---|
| **A** | 그대로 (넓게) | 0 — 여태 이 상태로 돌아왔다 |
| **B** | 철골렘·눈사람만 명시 제외 | 2줄. 「플레이어가 만든 것은 안 센다」는 규칙이 명확하다 |
| **C** | `MobCategory == monster` 로 좁힌다 | 모드 쪽에 `LS.isMonster(entity)` 다리 하나 — **이제 30분이면 된다** (이관 4단계로 다리에 51개를 더해 봤다) · 늑대·북극곰이 빠진다 |

~~**추천: B.**~~ (유저는 C 를 골랐다 — 위 참조) 규칙이 한 문장으로 설명되고("플레이어가 만든 것은 세상의 일부가 아니다"),
늑대·북극곰처럼 실제로 덤비는 놈들은 그대로 남는다. C 는 «덤비는데 안 세지는» 경우를 만든다.

---

## 3. ~~제련·수확 보너스 배율~~ — **이미 닫힌 결정 (2026-07-25)**

> 여기 «미결»로 올려두려다 확인해 보니 **2026-07-25 에 이미 정한 것**이었다.
> `TODO.md` E절: *「6 → 그대로 유지. 주석의 의도와 어긋난다는 지적을 알고도 현재 값을 택한 것이니,
> 나중에 채산성이 문제되면 이 결정을 다시 열면 된다 — 버그가 아니라 선택이다.」*
>
> 그런데 그 뒤 `SELF-CHECK` 가 이걸 다시 «자기모순»으로 올렸고, 나도 하마터면 또 올릴 뻔했다.
> **한 번 닫힌 결정이 근거 없이 두 번 다시 열렸다** — 이 문서가 필요한 이유가 이거다.

**남은 것은 결정이 아니라 정리다.** `TownEffects.java` 38~41행 주석이 여전히 2배를 거부하는
문장으로 남아 있어서, 읽는 사람마다 «버그다»로 판단하게 된다. 값이 아니라 **주석을 현재 결정에
맞춰 고쳐야 한다** — 이건 내가 그냥 한다(이 문서와 같은 커밋).

다시 열 조건: **채산성이 실제로 문제로 느껴질 때.** 그때 쓸 수 있게 계산만 남긴다.

| 가중치 | 기댓값 | 실배율 |
|---|---:|---:|
| `{33,24,24,19}` (지금) | +1.29 | 2.29배 |
| `{50,30,15,5}` | +0.75 | 1.75배 |
| `{60,30,8,2}` | +0.52 | 1.52배 |

---

## 4. 별의 축복 — 착수 전 3결정

설계 전문은 `docs/BLESSING.md`. 15종 부가효과가 전부 적혀 있고 구현만 안 됐다.

### 4-1. 부여 시점

| | 안 | 결과 |
|---|---|---|
| **A** | 각성마다 1개 (총 5개) | 5성이면 부가효과 5개 — 빌드 차이가 크게 벌어진다 |
| **B** | 3·5성에서만 (총 2개) | 5성의 «새 스킬 없는 빈 계단»을 메우면서 격차는 작게 |
| **C** | 5성에서만 (총 1개) | 최소 개입 · 5성 도달의 보상으로 명확 |

**추천: B.** 5성 계단을 메우는 게 원래 동기였고, 3성에 하나를 둬야 «다음이 있다»가 보인다.

### 4-2. 재감정

싸면 원하는 게 나올 때까지 돌리는 노가다가 되고, 비싸면 나쁜 걸 뽑은 사람이 그대로 굳는다.
5인 서버에서 한 명만 ⭐⭐⭐를 뽑으면 격차가 눈에 띈다.

| | 안 | 결과 |
|---|---|---|
| **A** | 없음 | 뽑기 한 번이 영구 — 운이 그대로 격차 |
| **B** | 균열 정수 3개 · 무제한 | 정수가 마을 재건과 경쟁하므로 «돌릴수록 마을이 늦어진다»가 비용이 된다 |
| **C** | 각성 단계당 1회만 (무료) | 노가다 불가 · 한 번의 만회 기회 |

**추천: C.** 정수를 비용으로 쓰면 결국 정수 파밍이 늘어난다. 횟수 제한이 노가다를
구조적으로 막는 유일한 방법이고, 「한 번은 다시 뽑을 수 있다」가 운의 격차도 충분히 줄인다.

### 4-3. ~~15종 전부 갈지~~ — **닫힘 (2026-07-31): 12종 먼저 · 전부 Java**

> 두 가지가 정해졌다.
>
> **① KubeJS 안은 뺀다.** 이 절이 원래 「12종은 KubeJS 로 된다」고 적고 있었는데, 그건
> 이관 4단계 전에 쓴 문장이다. `ARCHITECTURE.md` 원칙 1 —*「새 기능은 처음부터 모드로.
> KubeJS 는 이관 대상이지 확장 대상이 아니다」* — 이 그 뒤에 확정됐다. 축복을 스크립트로 짜면
> 유물 상태가 **모드 `custom_data` 와 스크립트 두 곳**에 생기는데, 그게 07-31 하루를 통째로
> 들여 없앤 구조다. `RelicEventHandlers` 에 걸어둘 훅이 이미 12개 있다.
>
> **② 12종 먼저.** 별의 도약·별똥 파편·재림 셋은 훅이 더 필요하다. 12종이면 조합이 이미
> 충분히 갈리고 검증할 표면이 작다. 굴려 보고 필요하면 붙인다.
> (이건 취향이 아니라 순서 문제라 내가 정했다 — 다르게 생각하면 다시 열 것.)

### 4-3-A. (원래 기록) 15종 전부 갈지

별의 도약·별똥 파편·재림 셋은 **Java 훅이 필요하다.** 나머지 12종은 KubeJS 로 된다.

| | 안 |
|---|---|
| **A** | 12종 먼저(KubeJS) → 3종 나중에 |
| **B** | 15종 한 번에 (Java 까지) |

**추천: A.** 12종이면 이미 조합이 충분히 갈린다. 남은 셋은 «있으면 좋은 것»이라
먼저 굴려 보고 필요하면 붙인다.

---

## 5. ~~RCON 포트 25575~~ — **이미 닫힌 결정 (2026-07-25) · 단, 조건부**

> `TODO.md` E절: *「7 → 방화벽 규칙 넣지 않음. 지금 로컬/사설망 전용이라는 전제에 기댄 것이다.」*

결정은 났다. 다만 그 결정에 **조건이 붙어 있다** — 다시 열 시점이 명시된 유일한 항목이다:

> ⚠️ **이 PC 를 친구들에게 개방하거나 포트포워딩하는 순간, 25575 를 반드시 막아야 한다.**
> 마인크래프트 바닐라에는 RCON 바인드 주소 설정이 없어 `0.0.0.0` 고정이고,
> RCON 은 **인증만 통과하면 서버 콘솔 전권**이다.

여기 적어두는 이유: 이건 «미결»이 아니라 **트리거**다. 미결 목록에 섞여 있으면
«아직 안 정했네»로 읽히고, 정작 열어야 할 순간에는 아무도 이 줄을 안 본다.
서버를 외부에 여는 작업을 시작하면 **그 작업의 첫 단계로** 이 줄을 꺼낼 것.

---

## 6. ~~`vGuideCont`~~ — **닫힘 (2026-07-31, B안 실행)**

`ls_voice.js` 의 죽은 헬퍼를 지웠다. 대사 10종 중 두 줄짜리가 하나도 없어 한 번도
불린 적이 없다. 수기 12편에서 필요해지면 한 줄이라 그때 다시 쓴다.

닫은 이유는 코드가 아니라 **스캐너 쪽**이다. 상시로 울리는 경보는 무시하게 되고,
그러면 진짜 죽은 코드가 생겼을 때 같이 묻힌다 — 그 위험은 실제로 이번에 증명됐다.
같은 날 스캐너 B절이 `ls_rift.js` 의 `ls_finale_armed` 를 잡았는데, 그게 없었으면
**마지막 봉인을 풀어도 「가장 긴 밤」이 영영 안 열리는** 상태로 나갔을 것이다.
경보를 깨끗하게 유지하는 값이 그만큼이다.

> ※ 이건 「고를 것」이 아니라 「한 것」이다. 이 절을 지우지 말고 닫힌 채로 둘 것 —
> 제련 배율이 두 번이나 다시 열린 게 **닫힌 기록을 안 남겨서**였다.

---

## 이미 결정된 것 (다시 열지 않기 위해)

| 항목 | 결정 | 날짜 |
|---|---|---|
| 밤 사냥 → 다음날 위협 -1 | **제외** (굳이 불필요) | 2026-07-24 |
| 제련·수확 배율 | **그대로 유지** (2.29배) — 주석과 어긋난다는 지적을 알고도 택함 | 2026-07-25 |
| RCON 방화벽 규칙 | **안 넣음** — 로컬/사설망 전제. 외부 개방 시 재검토(위 5번) | 2026-07-25 |
| 스틱스 백어택·크리 대역 50~70 | 유저가 직접 잡음 — **임의 변경 금지** | — |
| 파나케이아 유지 | 유지 | — |
| 측정 기준 | **성역 축복 안** 으로 통일 | — |
| 작업 진행 방식 | 단계마다 확인받지 말고 **바로 다음으로** | 2026-07-31 |

> **이 표가 이 문서의 진짜 목적이다.**
>
> 제련 배율은 07-25 에 «그대로 유지»로 닫혔는데, 그 뒤 `SELF-CHECK` 가 «자기모순»으로
> 다시 올렸고 오늘 나도 하마터면 세 번째로 올릴 뻔했다. RCON 도 같은 일을 겪었다.
>
> 닫힌 결정이 미결 목록으로 돌아오는 건 **결정에 날짜와 근거가 안 붙어 있어서**다.
> 근거가 없으면 다음에 그 코드를 읽는 사람(나 포함)이 «이거 버그 아닌가»부터 다시 시작한다.
> 그래서 이 표에는 항상 **날짜 + 무엇을 알고도 택했는지**를 같이 적는다.
